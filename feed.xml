<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko"><generator uri="https://jekyllrb.com/" version="4.2.2">Jekyll</generator><link href="https://mangbaam.github.io//jekyll-theme-yat/feed.xml" rel="self" type="application/atom+xml" /><link href="https://mangbaam.github.io//jekyll-theme-yat/" rel="alternate" type="text/html" hreflang="ko" /><updated>2022-07-23T18:16:40+00:00</updated><id>https://mangbaam.github.io//jekyll-theme-yat/feed.xml</id><title type="html">맹뱀 블로그</title><subtitle>안드로이드 개발자가 되기 위한 기록들</subtitle><author><name>mangbaam</name></author><entry><title type="html">Coroutine Context And Dispatchers</title><link href="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/23/coroutine-context-and-dispatchers.html" rel="alternate" type="text/html" title="Coroutine Context And Dispatchers" /><published>2022-07-23T00:00:00+00:00</published><updated>2022-07-23T00:00:00+00:00</updated><id>https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/23/coroutine-context-and-dispatchers</id><content type="html" xml:base="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/23/coroutine-context-and-dispatchers.html"><![CDATA[<h2>⭐</h2>

<p>코루틴은 항상 CoroutineContext 내에서 수행된다. 그리고 코루틴 컨텍스트는 다양한 요소(Elements)들로 구성된다.</p>

<h2 id="디스패처와-스레드">디스패처와 스레드</h2>

<hr />

<p>코루틴 컨텍스트는 연관된 코루틴들이 실행될 때 사용할 스레드나 스레드 풀을 결정하는 코루틴 디스패처를 포함한다. 특정 스레드에서 동작하게 하거나, 특정 스레드 풀로 전달하거나, 스레드 제한 없이 실행되도록 할 수도 있다.</p>

<p>모든 코루틴 빌더를 사용할 때 파라미터로 CoroutineContext를 넘겨줄 수 있고, 이를 이용해 새로운 코루틴들을 위한 디스패쳐나 그 외의 컨텍스트 요소를 지정할 수 있다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">launch1</span><span class="p">():</span> <span class="nc">Unit</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="nf">launch</span> <span class="p">{</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"main runBlocking - thread ${Thread.currentThread().name}"</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="nf">launch</span><span class="p">(</span><span class="nc">Dispatchers</span><span class="p">.</span><span class="nc">Unconfined</span><span class="p">)</span> <span class="p">{</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"Unconfined - thread ${Thread.currentThread().name}"</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="nf">launch</span><span class="p">(</span><span class="nc">Dispatchers</span><span class="p">.</span><span class="nc">Default</span><span class="p">)</span> <span class="p">{</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"Default - thread ${Thread.currentThread().name}"</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="nf">launch</span><span class="p">(</span><span class="nf">newSingleThreadContext</span><span class="p">(</span><span class="s">"NewThread"</span><span class="p">))</span> <span class="p">{</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"newSingleThreadContext - thread ${Thread.currentThread().name}"</span><span class="p">)</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><img src="https://user-images.githubusercontent.com/44221447/180615270-9c196944-84e3-4fd1-b9de-0d2647c2c0cf.png" alt="result1" /></p>

<p>첫 번째 launch 에서는 파라미터를 지정하지 않았다. 이처럼 파라미터로 디스패처를 지정하지 않은 경우 실행된 코루틴 스코프로부터 상속 받은 컨텍스트를 사용한다. 여기서는 runBlocking의 컨텍스트를 사용한다.</p>

<p>두 번재 launch 에서는 <code class="language-plaintext highlighter-rouge">Unconfined</code>를 넘겨줬다. 이는 아래서 살펴보고자 한다.</p>

<p>세 번재 launch 에서는 <code class="language-plaintext highlighter-rouge">Default</code>를 넘겨줬다. 이는 GlobalScope에서 실행될 경우 사용되며, 공통으로 사용되는 백그라운드 스레드 풀을 사용한다. 즉 <code class="language-plaintext highlighter-rouge">launch(Dispathcers.Defult) { ... }</code> 와 <code class="language-plaintext highlighter-rouge">GlobalScope.launch { ... }</code>는 동일한 디스패처를 사용한다.</p>

<p>네 번째 launch 에서는 <code class="language-plaintext highlighter-rouge">newSingleThreadContext</code>를 넘겨줬다. 이는 항상 새로운 스레드를 생성한다. 이처럼 전용 스레드를 생성하는 작업은 더 이상 사용되지 않을 때는 close() 함수로 해제하거나 Top-level 변수에 저장해 어플리케이션 전반에서 사용되어야 한다.</p>

<h3 id="unconfined-vs-confined-dispatcher">Unconfined vs confined dispatcher</h3>

<p>위 코드에서 두 번째에 <code class="language-plaintext highlighter-rouge">Unconfined</code>를 넘겨줬다. <code class="language-plaintext highlighter-rouge">Dispathcers.Unconfined</code>는 호출 스레드에서 코루틴을 시작하고, 처음으로 중단 지점을 만난 후 다시 재개될 때는 중단 함수를 재개한 스레드에서 수행된다.</p>

<p>Unconfined 디스패처는 코루틴이 CPU 시간을 소모하지 않거나 UI를 업데이트 하지 않는 등의 상황에서 특정 스레드에 국한된 작업이 아닌 경우에 적절하다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">():</span> <span class="nc">Unit</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="nf">launch</span><span class="p">(</span><span class="nc">Dispatchers</span><span class="p">.</span><span class="nc">Unconfined</span><span class="p">)</span> <span class="p">{</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"Unconfined - thread ${Thread.currentThread().name}"</span><span class="p">)</span>
        <span class="nf">delay</span><span class="p">(</span><span class="mi">500</span><span class="p">)</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"Unconfined- thread ${Thread.currentThread().name}"</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="nf">launch</span> <span class="p">{</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"main runBlocking - thread ${Thread.currentThread().name}"</span><span class="p">)</span>
        <span class="nf">delay</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"main runBlocking After delay - thread ${Thread.currentThread().name}"</span><span class="p">)</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><img src="https://user-images.githubusercontent.com/44221447/180615558-ee2a07aa-5d0d-4c6e-b17a-26d2d23e2b5d.png" alt="result2" /></p>

<p>runBlocking { … } 컨텍스트를 상속한 코루틴은 메인 스레드에서 수행되지만 unconfined 코루틴은 delay 함수가 사용되는 <code class="language-plaintext highlighter-rouge">DefaultExecutor</code> 스레드에서 실행되는 것을 확인할 수 있다.</p>

<blockquote>
  <p>여기서 Unconfined에 대해 소개하기는 했지만 잘못 사용하는 경우 예상치 못한 동작을 할 수 있기 때문에 특수한 경우를 제외하고는 사용을 지양해야 한다.</p>
</blockquote>

<h2 id="코루틴-스레드-디버깅">코루틴, 스레드 디버깅</h2>

<hr />

<p>코루틴이 언제 어디서 수행 중이었는지를 파악하기는 쉽지 않다. 일반적으로 로그를 사용해서 확인하게 되는데 위 예제들처럼 스레드 이름만을 사용하면 구분하기 쉽지 않을 수 있다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="nn">kotlinx.coroutines.async</span>
<span class="k">import</span> <span class="nn">kotlinx.coroutines.runBlocking</span>

<span class="k">fun</span> <span class="nf">log</span><span class="p">(</span><span class="n">msg</span><span class="p">:</span> <span class="nc">String</span><span class="p">)</span> <span class="p">=</span> <span class="nf">println</span><span class="p">(</span><span class="s">"[${Thread.currentThread().name}] $msg"</span><span class="p">)</span>

<span class="k">fun</span> <span class="nf">main</span><span class="p">()</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">a</span> <span class="p">=</span> <span class="nf">async</span> <span class="p">{</span>
        <span class="nf">log</span><span class="p">(</span><span class="s">"async A return 6"</span><span class="p">)</span>
        <span class="mi">6</span>
    <span class="p">}</span>
    <span class="kd">val</span> <span class="py">b</span> <span class="p">=</span> <span class="nf">async</span> <span class="p">{</span>
        <span class="nf">log</span><span class="p">(</span><span class="s">"async B return 7"</span><span class="p">)</span>
        <span class="mi">7</span>
    <span class="p">}</span>
    <span class="nf">log</span><span class="p">(</span><span class="s">"The answer is ${a.await() * b.await()}"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p><img src="https://user-images.githubusercontent.com/44221447/180616165-ec810c6e-d722-4042-a5ac-c7d084fb6de9.png" alt="debug result1" /></p>

<p>현재는 위와 같이 main 이라는 스레드 이름만이 나오게 된다.</p>

<p>다음 방법으로 로그에서 코루틴의 이름까지 출력할 수 있다. (출처 : <a href="https://stackoverflow.com/questions/52049150/dkotlinx-coroutines-debug-not-working-in-intellij-idea-jvm-options">스택오버플로우</a>)</p>

<p><em>한 번 실행 시켜야 설정할 수 있다.</em></p>

<ol>
  <li>Run - Edit Configurations</li>
</ol>

<p><img src="https://user-images.githubusercontent.com/44221447/180616417-e7815811-7c5f-49d6-8635-83df322d033c.png" alt="setting1" /></p>

<ol>
  <li>VM options</li>
</ol>

<p><img src="https://user-images.githubusercontent.com/44221447/180616326-839b8264-a6f5-4f5a-83be-98cb660e50c6.png" alt="configurations" /></p>

<blockquote>
  <p>-Dkotlinx.coroutines.debug=on</p>
</blockquote>

<ol>
  <li>Result</li>
</ol>

<p><img src="https://user-images.githubusercontent.com/44221447/180616471-b1fd8328-f59f-4b15-9e84-d0888e071515.png" alt="debug result 2" /></p>

<p>위와 동일한 코드를 실행해보면 main 만 나오는 것이 아니라 코루틴 정보까지 나와서 동일한 코루틴에서 동작하고 있는지 아닌지를 확인할 수 있다.</p>

<p>좀 더 많은 정보는 <a href="https://kotlin.github.io/kotlinx.coroutines/kotlinx-coroutines-core/kotlinx.coroutines/new-coroutine-context.html">여기</a>서 확인할 수 있다.</p>

<h2 id="스레드-전환">스레드 전환</h2>

<hr />

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="nn">kotlinx.coroutines.newSingleThreadContext</span>
<span class="k">import</span> <span class="nn">kotlinx.coroutines.runBlocking</span>
<span class="k">import</span> <span class="nn">kotlinx.coroutines.withContext</span>

<span class="k">fun</span> <span class="nf">main</span><span class="p">()</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="nf">newSingleThreadContext</span><span class="p">(</span><span class="s">"Ctx1"</span><span class="p">).</span><span class="nf">use</span> <span class="p">{</span> <span class="n">ctx1</span> <span class="p">-&gt;</span>
        <span class="nf">newSingleThreadContext</span><span class="p">(</span><span class="s">"Ctx2"</span><span class="p">).</span><span class="nf">use</span> <span class="p">{</span> <span class="n">ctx2</span> <span class="p">-&gt;</span>
            <span class="nf">runBlocking</span><span class="p">(</span><span class="n">ctx1</span><span class="p">)</span> <span class="p">{</span>
                <span class="nf">log</span><span class="p">(</span><span class="s">"Started in ctx1"</span><span class="p">)</span>
                <span class="nf">withContext</span><span class="p">(</span><span class="n">ctx2</span><span class="p">)</span> <span class="p">{</span>
                    <span class="nf">log</span><span class="p">(</span><span class="s">"Working in ctx2"</span><span class="p">)</span>
                <span class="p">}</span>
                <span class="nf">log</span><span class="p">(</span><span class="s">"Back to ctx1"</span><span class="p">)</span>
            <span class="p">}</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><img src="https://user-images.githubusercontent.com/44221447/180616672-f3ff3638-829a-45ad-b77e-3dee68b00e60.png" alt="trans result1" /></p>

<p><code class="language-plaintext highlighter-rouge">newSingleThreadContext</code>는 새로운 스레드를 생성한다고 설명했고, 반드시 해제해주어야 한다고 했다. 코틀린의 <code class="language-plaintext highlighter-rouge">use</code> 문을 사용하면 사용 후 자동으로 해제해주기 때문에 이 상황에서 유용하게 사용된다.</p>

<p>코드와 결과를 보면 “Started in ctx1” 부분은 두 번째 생성된 코루틴이라서 <strong>@coroutine#2</strong> 에서 실행되지만 runBlocking의 인자로 들어온 ctx1 컨텍스트에서 실행된다.</p>

<p>하지만 동일한 코루틴에 있는 “Working in ctx2” 부분은 withContext()로 인해서 ctx2 컨텍스트에서 실행된다. 그리고 코루틴을 보면 기존 코루틴(@coroutine#2)을 유지하고 있다.</p>

<h2 id="context의-job-확인">context의 Job 확인</h2>

<hr />

<p>코루틴의 Job은 그 컨텍스트의 일부이다. <code class="language-plaintext highlighter-rouge">coroutineContext[Job]</code>으로 Job을 확인할 수 있다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="nn">kotlinx.coroutines.Job</span>
<span class="k">import</span> <span class="nn">kotlinx.coroutines.runBlocking</span>

<span class="k">fun</span> <span class="nf">main</span><span class="p">()</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"My job is ${coroutineContext[Job]}"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p><img src="https://user-images.githubusercontent.com/44221447/180616989-ebc5220d-41fe-42d8-8965-b965f14081f1.png" alt="job result" /></p>

<h2 id="코루틴의-자식">코루틴의 자식</h2>

<hr />

<p>어떤 코루틴(부모 코루틴) 스코프 안에서 다른 코루틴(자식 코루틴)을 생성하고 실행하면 부모 코루틴의 CoroutineScope.coroutinecontext 를 통해 컨텍스트를 상속하고, 새롭게 생성된 자식 코루틴의 Job은 부모 코루틴 Job의 자식으로 생성된다.</p>

<p>만약 부모 코루틴이 취소되면 모든 자식 코루틴 역시 재귀적으로 취소된다.</p>

<p>예외적으로 GlobalScope에서 실행된 코루틴은 어디서 실행되건 독립적으로 동작한다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="nn">kotlinx.coroutines.GlobalScope</span>
<span class="k">import</span> <span class="nn">kotlinx.coroutines.delay</span>
<span class="k">import</span> <span class="nn">kotlinx.coroutines.launch</span>
<span class="k">import</span> <span class="nn">kotlinx.coroutines.runBlocking</span>

<span class="k">fun</span> <span class="nf">main</span><span class="p">()</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">request</span> <span class="p">=</span> <span class="nf">launch</span> <span class="p">{</span>
        <span class="nc">GlobalScope</span><span class="p">.</span><span class="nf">launch</span> <span class="p">{</span>
            <span class="nf">println</span><span class="p">(</span><span class="s">"job1: I run in GlobalScope and execute independently!"</span><span class="p">)</span>
            <span class="nf">delay</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>
            <span class="nf">println</span><span class="p">(</span><span class="s">"job1: I am not affected by cancellation of the request"</span><span class="p">)</span>
        <span class="p">}</span>
        <span class="nf">launch</span> <span class="p">{</span>
            <span class="nf">delay</span><span class="p">(</span><span class="mi">100</span><span class="p">)</span>
            <span class="nf">println</span><span class="p">(</span><span class="s">"job2: I am a child of the request coroutine"</span><span class="p">)</span>
            <span class="nf">delay</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>
            <span class="nf">println</span><span class="p">(</span><span class="s">"job2: I will not execute this line if my parent request is cancelled"</span><span class="p">)</span>
        <span class="p">}</span>
    <span class="p">}</span>
    <span class="nf">delay</span><span class="p">(</span><span class="mi">500</span><span class="p">)</span>
    <span class="n">request</span><span class="p">.</span><span class="nf">cancel</span><span class="p">()</span>
    <span class="nf">delay</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"main: Who has survived request cancellation?"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p><img src="https://user-images.githubusercontent.com/44221447/180617184-47ae14b8-b4f3-4532-b09e-6debe24c41df.png" alt="global result" /></p>

<p>request가 취소된 후에도 GlobalScope에서 실행된 코루틴은 살아있는 것을 확인할 수 있다.</p>

<h2 id="부모-코루틴의-의무">부모 코루틴의 의무</h2>

<hr />

<p>부모 코루틴은 모든 자식 코루틴이 실행 완료 될 때까지 기다린다. 이때 <code class="language-plaintext highlighter-rouge">Job.join()</code> 함수를 사용할 필요가 없다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">()</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>

    <span class="kd">val</span> <span class="py">request</span> <span class="p">=</span> <span class="nf">launch</span> <span class="p">{</span>
        <span class="nf">repeat</span><span class="p">(</span><span class="mi">3</span><span class="p">)</span> <span class="p">{</span> <span class="n">i</span> <span class="p">-&gt;</span> <span class="c1">// launch a few children jobs</span>
            <span class="nf">launch</span>  <span class="p">{</span>
                <span class="nf">delay</span><span class="p">((</span><span class="n">i</span> <span class="p">+</span> <span class="mi">1</span><span class="p">)</span> <span class="p">*</span> <span class="mi">200L</span><span class="p">)</span> <span class="c1">// variable delay 200ms, 400ms, 600ms</span>
                <span class="nf">println</span><span class="p">(</span><span class="s">"Coroutine $i is done"</span><span class="p">)</span>
            <span class="p">}</span>
        <span class="p">}</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"request: I'm done and I don't explicitly join my children that are still active"</span><span class="p">)</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><img src="https://user-images.githubusercontent.com/44221447/180617380-1724a5d1-09de-403c-b1c0-c128e47be06b.png" alt="wait result" /></p>

<p>request는 3개의 자식 코루틴을 만들고 launch를 하지 않았음에도 모든 자식 코루틴이 실행된 후에 종료되었다.</p>

<h2 id="디버깅을-위해-코루틴-이름-지어주기">디버깅을 위해 코루틴 이름 지어주기</h2>

<hr />

<p>디버깅 할 때 기본적으로 자동 할당된 id를 확인할 수 있는데 어떤 코루틴인지 확실하게 알기 위해서 이름을 지정하면 좋다.</p>

<p>CoroutineName 이라는 컨텍스트 요소를 통해 코루틴 이름을 지어줄 수 있다. 디버깅 모드일 때 CoroutineName이 수행 중인 스레드 이름과 함께 코루틴 이름도 나타난다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="nn">kotlinx.coroutines.CoroutineName</span>
<span class="k">import</span> <span class="nn">kotlinx.coroutines.async</span>
<span class="k">import</span> <span class="nn">kotlinx.coroutines.delay</span>
<span class="k">import</span> <span class="nn">kotlinx.coroutines.runBlocking</span>

<span class="k">fun</span> <span class="nf">main</span><span class="p">()</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="nf">log</span><span class="p">(</span><span class="s">"Started main coroutine"</span><span class="p">)</span>
    <span class="kd">val</span> <span class="py">v1</span> <span class="p">=</span> <span class="nf">async</span><span class="p">(</span><span class="nc">CoroutineName</span><span class="p">(</span><span class="s">"v1coroutine"</span><span class="p">))</span> <span class="p">{</span>
        <span class="nf">delay</span><span class="p">(</span><span class="mi">500</span><span class="p">)</span>
        <span class="nf">log</span><span class="p">(</span><span class="s">"Computing v1"</span><span class="p">)</span>
        <span class="mi">252</span>
    <span class="p">}</span>
    <span class="kd">val</span> <span class="py">v2</span> <span class="p">=</span> <span class="nf">async</span><span class="p">(</span><span class="nc">CoroutineName</span><span class="p">(</span><span class="s">"v2coroutine"</span><span class="p">))</span> <span class="p">{</span>
        <span class="nf">delay</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>
        <span class="nf">log</span><span class="p">(</span><span class="s">"Computing v2"</span><span class="p">)</span>
        <span class="mi">6</span>
    <span class="p">}</span>
    <span class="nf">log</span><span class="p">(</span><span class="s">"The answer for v1 / v2 = ${v1.await() / v2.await()}"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p><img src="https://user-images.githubusercontent.com/44221447/180617523-ace76166-c8a1-49e8-b58e-90fe343fb453.png" alt="naming result" /></p>

<h2 id="컨텍스트-요소-병합">컨텍스트 요소 병합</h2>

<hr />

<p>코루틴 컨텍스트 요소를 두개 이상 동시에 지정해야 하는 경우 +(plus) 연산자를 통해 병합할 수 있다. 예를 들어 디스패처를 지정하면서 코루틴 이름을 지정할 수 있다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="nn">kotlinx.coroutines.CoroutineName</span>
<span class="k">import</span> <span class="nn">kotlinx.coroutines.Dispatchers</span>
<span class="k">import</span> <span class="nn">kotlinx.coroutines.launch</span>
<span class="k">import</span> <span class="nn">kotlinx.coroutines.runBlocking</span>

<span class="k">fun</span> <span class="nf">main</span><span class="p">()</span> <span class="p">=</span> <span class="n">runBlocking</span><span class="p">&lt;</span><span class="nc">Unit</span><span class="p">&gt;</span> <span class="p">{</span>

    <span class="nf">launch</span><span class="p">(</span><span class="nc">Dispatchers</span><span class="p">.</span><span class="nc">Default</span> <span class="p">+</span> <span class="nc">CoroutineName</span><span class="p">(</span><span class="s">"test"</span><span class="p">))</span> <span class="p">{</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"I'm working in thread ${Thread.currentThread().name}"</span><span class="p">)</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><img src="https://user-images.githubusercontent.com/44221447/180617629-9b6fae1a-4bd4-4694-b8d4-30e1be3581fa.png" alt="combine result" /></p>]]></content><author><name>mangbaam</name></author><category term="Kotlin" /><category term="coroutine" /><category term="dispatcher" /><category term="context" /><summary type="html"><![CDATA[⭐]]></summary></entry><entry><title type="html">How to get longest continuous list in kotlin</title><link href="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/19/kotlin-contiuous-list.html" rel="alternate" type="text/html" title="How to get longest continuous list in kotlin" /><published>2022-07-19T00:00:00+00:00</published><updated>2022-07-19T00:00:00+00:00</updated><id>https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/19/kotlin-contiuous-list</id><content type="html" xml:base="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/19/kotlin-contiuous-list.html"><![CDATA[<h2>⭐</h2>

<p>이번에 주어진 정수형 배열에서 가장 긴 연속된 배열을 찾아야하는 작업이 있어서 그 방법에 대해 찾아보게 되었다.</p>

<h2 id="fold-vs-reduce">fold vs reduce</h2>

<hr />

<p>1, 2, 3, 5, 7, 8, 9, 10, 11, 13 을 가진 리스트를 위 코드로 수행하면 1, 2, 3 / 5 / 7, 8, 9, 10, 11 / 13 으로 쪼갤 수 있다.</p>

<p>kotlin의 <code class="language-plaintext highlighter-rouge">fold</code>는 <code class="language-plaintext highlighter-rouge">reduce</code>와 비슷한 동작을 하는데 reduce는 초기 값을 받지 않는 반면 fold는 초기 값을 받게 된다.</p>

<p><em>reduce</em></p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">public</span> <span class="k">inline</span> <span class="k">fun</span> <span class="p">&lt;</span><span class="nc">S</span><span class="p">,</span> <span class="nc">T</span> <span class="p">:</span> <span class="nc">S</span><span class="p">&gt;</span> <span class="nf">Iterable</span><span class="p">&lt;</span><span class="nc">T</span><span class="p">&gt;.</span><span class="nf">reduce</span><span class="p">(</span>
    <span class="n">operation</span><span class="p">:</span> <span class="p">(</span><span class="nc">S</span><span class="p">,</span> <span class="nc">T</span><span class="p">)</span> <span class="p">-&gt;</span> <span class="nc">S</span>
<span class="p">):</span> <span class="nc">S</span>
</code></pre></div></div>

<p><em>fold</em></p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">public</span> <span class="k">inline</span> <span class="k">fun</span> <span class="p">&lt;</span><span class="nc">T</span><span class="p">,</span> <span class="nc">R</span><span class="p">&gt;</span> <span class="nf">Iterable</span><span class="p">&lt;</span><span class="nc">T</span><span class="p">&gt;.</span><span class="nf">fold</span><span class="p">(</span>
    <span class="n">initial</span><span class="p">:</span> <span class="nc">R</span><span class="p">,</span>
    <span class="n">operation</span><span class="p">:</span> <span class="p">(</span><span class="nc">R</span><span class="p">,</span> <span class="nc">T</span><span class="p">)</span> <span class="p">-&gt;</span> <span class="nc">R</span>
<span class="p">):</span> <span class="nc">R</span>
</code></pre></div></div>

<p>reduce는 초기 값을 받지 않는 대신 컬렉션의 <strong>첫 번째 요소</strong>를 초기 값으로 사용하고, 자연스럽게 반환 타입도 <strong>컬렉션의 자료형</strong>이 된다.</p>

<p>반면 fold는 초기 값을 받게 되면서 반환 타입은 <strong>초기 값의 자료형</strong>이 된다.</p>

<h2 id="do-it">do it</h2>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">simpleList</span> <span class="p">=</span> <span class="nf">mutableListOf</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">7</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">9</span><span class="p">,</span> <span class="mi">10</span><span class="p">,</span> <span class="mi">11</span><span class="p">,</span> <span class="mi">13</span><span class="p">)</span>
    <span class="kd">val</span> <span class="py">continuous</span> <span class="p">=</span> <span class="n">simpleList</span><span class="p">.</span><span class="nf">fold</span><span class="p">(</span><span class="n">mutableListOf</span><span class="p">&lt;</span><span class="nc">MutableList</span><span class="p">&lt;</span><span class="nc">Int</span><span class="p">&gt;&gt;())</span> <span class="p">{</span> <span class="n">acc</span><span class="p">,</span> <span class="n">i</span> <span class="p">-&gt;</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">acc</span><span class="p">.</span><span class="nf">isEmpty</span><span class="p">()</span> <span class="p">||</span> <span class="n">acc</span><span class="p">.</span><span class="nf">last</span><span class="p">().</span><span class="nf">last</span><span class="p">()</span> <span class="p">!=</span> <span class="n">i</span> <span class="p">-</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">acc</span><span class="p">.</span><span class="nf">add</span><span class="p">(</span><span class="nf">mutableListOf</span><span class="p">(</span><span class="n">i</span><span class="p">))</span>
        <span class="p">}</span> <span class="k">else</span> <span class="n">acc</span><span class="p">.</span><span class="nf">last</span><span class="p">().</span><span class="nf">add</span><span class="p">(</span><span class="n">i</span><span class="p">)</span>
        <span class="n">acc</span>
    <span class="p">}</span>
    <span class="nf">println</span><span class="p">(</span><span class="n">continuous</span><span class="p">)</span> <span class="c1">// [[1, 2, 3], [5], [7, 8, 9, 10, 11], [13]]</span>
<span class="p">}</span>
</code></pre></div></div>

<p>fold의 초기 값으로 빈 이중 리스트를 줬다. 이때 acc는 초기 값으로 받은 이중 리스트를 의미하고, i는 simpleList의 요소를 순차 접근한다.</p>

<p>그래서 맨 처음에 acc가 비어있을 때와 앞선 요소가 연속되지 않은 경우 acc에 새 리스트에 i를 담은 것을 추가하고, 앞선 요소가 연속되는 경우는 acc의 마지막 리스트의 맨 끝에 i를 추가한다.</p>

<p>이 중에서 가장 긴 리스트를 구하고, 가장 긴 리스트가 여러 개 있다면 마지막 요소가 가장 큰 리스트를 구해보려면 다음과 같이 하면 된다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">simpleList</span> <span class="p">=</span> <span class="nf">mutableListOf</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">7</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">9</span><span class="p">,</span> <span class="mi">10</span><span class="p">,</span> <span class="mi">11</span><span class="p">,</span> <span class="mi">13</span><span class="p">)</span>
    <span class="kd">val</span> <span class="py">longest</span> <span class="p">=</span> <span class="n">simpleList</span><span class="p">.</span><span class="nf">fold</span><span class="p">(</span><span class="n">mutableListOf</span><span class="p">&lt;</span><span class="nc">MutableList</span><span class="p">&lt;</span><span class="nc">Int</span><span class="p">&gt;&gt;())</span> <span class="p">{</span> <span class="n">acc</span><span class="p">,</span> <span class="n">i</span> <span class="p">-&gt;</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">acc</span><span class="p">.</span><span class="nf">isEmpty</span><span class="p">()</span> <span class="p">||</span> <span class="n">acc</span><span class="p">.</span><span class="nf">last</span><span class="p">().</span><span class="nf">last</span><span class="p">()</span> <span class="p">!=</span> <span class="n">i</span> <span class="p">-</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">acc</span><span class="p">.</span><span class="nf">add</span><span class="p">(</span><span class="nf">mutableListOf</span><span class="p">(</span><span class="n">i</span><span class="p">))</span>
        <span class="p">}</span> <span class="k">else</span> <span class="n">acc</span><span class="p">.</span><span class="nf">last</span><span class="p">().</span><span class="nf">add</span><span class="p">(</span><span class="n">i</span><span class="p">)</span>
        <span class="n">acc</span>
    <span class="p">}.</span><span class="nf">sortedWith</span><span class="p">(</span><span class="nf">compareBy</span><span class="p">({</span> <span class="n">it</span><span class="p">.</span><span class="n">size</span> <span class="p">},</span> <span class="p">{</span> <span class="n">it</span><span class="p">.</span><span class="nf">last</span><span class="p">()</span> <span class="p">})).</span><span class="nf">last</span><span class="p">()</span>
    <span class="nf">println</span><span class="p">(</span><span class="n">longest</span><span class="p">)</span> <span class="c1">// [7, 8, 9, 10, 11]</span>
<span class="p">}</span>
</code></pre></div></div>

<p>2개의 조건으로 정렬하는 방법은 <a href="https://mangbaam.github.io/kotlin/2022/07/18/kotlin-sortwith.html">이전 게시글</a>에서 다뤘다.</p>

<p>크기와 마지막 요소를 기준으로 정렬한 후에 마지막 아이템(리스트)를 취하면 된다.</p>]]></content><author><name>mangbaam</name></author><category term="Kotlin" /><category term="kotlin" /><category term="tip" /><category term="basic" /><summary type="html"><![CDATA[⭐]]></summary></entry><entry><title type="html">How to sort List&amp;lt;Pair&amp;lt;Int, Int» in kotlin</title><link href="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/18/kotlin-sortwith.html" rel="alternate" type="text/html" title="How to sort List&amp;lt;Pair&amp;lt;Int, Int» in kotlin" /><published>2022-07-18T00:00:00+00:00</published><updated>2022-07-18T00:00:00+00:00</updated><id>https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/18/kotlin-sortwith</id><content type="html" xml:base="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/18/kotlin-sortwith.html"><![CDATA[<h2>⭐</h2>

<p>이번에 <code class="language-plaintext highlighter-rouge">List&lt;Pair&lt;Int, Int&gt;&gt;</code> 타입을 정렬해 줄 경우가 생겨서 알아보게 된 내용이다. 정확히는 <code class="language-plaintext highlighter-rouge">Map&lt;Int, Int&gt;</code> 자료 구조를 value - key 순으로 정렬하고자 했는데 Map의 아이템으로 Pair가 사용되기 때문에 같은 내용으로 생각해도 될 것 같다.</p>

<p>Pair는 2개의 아이템이 하나의 쌍으로 되어있는 자료 구조인데, 첫 번째 아이템을 first, 두 번째 아이템을 second 라고 하자. (<code class="language-plaintext highlighter-rouge">first</code>, <code class="language-plaintext highlighter-rouge">second</code>로 각각 첫 번째, 두 번째 아이템에 접근할 수 있다)</p>

<p>이때 <code class="language-plaintext highlighter-rouge">second</code>를 기준으로 정렬 후 <code class="language-plaintext highlighter-rouge">second</code> 값이 같은 것끼리는 <code class="language-plaintext highlighter-rouge">first</code>를 기준으로 정렬하고자 한다.</p>

<h2 id="sortedwith">sortedWith</h2>

<hr />

<p>많이 사용하는 <code class="language-plaintext highlighter-rouge">sortedBy</code>와 달리 <code class="language-plaintext highlighter-rouge">sortedWith</code>는 <strong>comparator</strong>를 지정해서 다중 조건을 둘 수 있다는 장점이 있다. 이러한 특징 때문에 Pair의 두 아이템을 각각 기준으로 만들어 주기 위해서 <code class="language-plaintext highlighter-rouge">sortedWith</code>를 사용했다.</p>

<p>방법은 간단하다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">simpleMap</span> <span class="p">=</span> <span class="n">mutableMapOf</span><span class="p">&lt;</span><span class="nc">Int</span><span class="p">,</span> <span class="nc">Int</span><span class="p">&gt;(</span>
        <span class="mi">1</span> <span class="n">to</span> <span class="mi">10</span><span class="p">,</span>
        <span class="mi">2</span> <span class="n">to</span> <span class="mi">20</span><span class="p">,</span>
        <span class="mi">3</span> <span class="n">to</span> <span class="mi">30</span><span class="p">,</span>
        <span class="mi">4</span> <span class="n">to</span> <span class="mi">30</span><span class="p">,</span>
        <span class="mi">5</span> <span class="n">to</span> <span class="mi">30</span>
    <span class="p">)</span>
    <span class="kd">val</span> <span class="py">sortedMap</span> <span class="p">=</span> <span class="n">simpleMap</span><span class="p">.</span><span class="nf">toList</span><span class="p">().</span><span class="nf">sortedWith</span><span class="p">(</span><span class="nf">compareBy</span><span class="p">({</span> <span class="n">it</span><span class="p">.</span><span class="n">second</span> <span class="p">},</span> <span class="p">{</span> <span class="n">it</span><span class="p">.</span><span class="n">first</span> <span class="p">}))</span>
    <span class="nf">println</span><span class="p">(</span><span class="n">sortedMap</span><span class="p">)</span> <span class="c1">// [(1, 10), (2, 20), (3, 30), (4, 30), (5, 30)]</span>
<span class="p">}</span>
</code></pre></div></div>

<p>정렬된 값 중 가장 큰 값을 구하는 방법은 다음과 같다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">simpleMap</span> <span class="p">=</span> <span class="n">mutableMapOf</span><span class="p">&lt;</span><span class="nc">Int</span><span class="p">,</span> <span class="nc">Int</span><span class="p">&gt;(</span>
        <span class="mi">1</span> <span class="n">to</span> <span class="mi">10</span><span class="p">,</span>
        <span class="mi">2</span> <span class="n">to</span> <span class="mi">20</span><span class="p">,</span>
        <span class="mi">3</span> <span class="n">to</span> <span class="mi">30</span><span class="p">,</span>
        <span class="mi">4</span> <span class="n">to</span> <span class="mi">30</span><span class="p">,</span>
        <span class="mi">5</span> <span class="n">to</span> <span class="mi">30</span>
    <span class="p">)</span>
    <span class="kd">val</span> <span class="py">largestMap</span> <span class="p">=</span> <span class="n">simpleMap</span><span class="p">.</span><span class="nf">toList</span><span class="p">().</span><span class="nf">sortedWith</span><span class="p">(</span><span class="nf">compareBy</span><span class="p">({</span> <span class="n">it</span><span class="p">.</span><span class="n">second</span> <span class="p">},</span> <span class="p">{</span> <span class="n">it</span><span class="p">.</span><span class="n">first</span> <span class="p">})).</span><span class="nf">last</span><span class="p">()</span>
    <span class="nf">println</span><span class="p">(</span><span class="n">largestMap</span><span class="p">)</span> <span class="c1">// (5, 30)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>그냥 마지막 아이템만 취해주면 된다.</p>

<p>내림차순으로 정렬하기 위한 방법은 다음과 같다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">simpleMap</span> <span class="p">=</span> <span class="n">mutableMapOf</span><span class="p">&lt;</span><span class="nc">Int</span><span class="p">,</span> <span class="nc">Int</span><span class="p">&gt;(</span>
        <span class="mi">1</span> <span class="n">to</span> <span class="mi">10</span><span class="p">,</span>
        <span class="mi">2</span> <span class="n">to</span> <span class="mi">20</span><span class="p">,</span>
        <span class="mi">3</span> <span class="n">to</span> <span class="mi">30</span><span class="p">,</span>
        <span class="mi">4</span> <span class="n">to</span> <span class="mi">30</span><span class="p">,</span>
        <span class="mi">5</span> <span class="n">to</span> <span class="mi">30</span>
    <span class="p">)</span>
    <span class="kd">val</span> <span class="py">reverseSortedMap</span> <span class="p">=</span> <span class="n">simpleMap</span><span class="p">.</span><span class="nf">toList</span><span class="p">().</span><span class="nf">sortedWith</span><span class="p">(</span><span class="nf">compareBy</span><span class="p">({</span> <span class="n">it</span><span class="p">.</span><span class="n">second</span> <span class="p">},</span> <span class="p">{</span> <span class="n">it</span><span class="p">.</span><span class="n">first</span> <span class="p">})).</span><span class="nf">asReversed</span><span class="p">()</span>
    <span class="nf">println</span><span class="p">(</span><span class="n">reverseSortedMap</span><span class="p">)</span> <span class="c1">// [(5, 30), (4, 30), (3, 30), (2, 20), (1, 10)]</span>
<span class="p">}</span>
</code></pre></div></div>]]></content><author><name>mangbaam</name></author><category term="Kotlin" /><category term="basic" /><category term="tip" /><category term="kotlin" /><summary type="html"><![CDATA[⭐]]></summary></entry><entry><title type="html">Kotlin Coroutine Cancellation</title><link href="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/15/coroutine-cancellation.html" rel="alternate" type="text/html" title="Kotlin Coroutine Cancellation" /><published>2022-07-15T00:00:00+00:00</published><updated>2022-07-15T00:00:00+00:00</updated><id>https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/15/coroutine-cancellation</id><content type="html" xml:base="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/15/coroutine-cancellation.html"><![CDATA[<h2>⭐</h2>

<blockquote>
  <p>본 글은 <a href="https://myungpyo.medium.com/%EC%BD%94%EB%A3%A8%ED%8B%B4-%EA%B3%B5%EC%8B%9D-%EA%B0%80%EC%9D%B4%EB%93%9C-%EC%9E%90%EC%84%B8%ED%9E%88-%EC%9D%BD%EA%B8%B0-part-2-ccd47699b520">코루틴 공식 가이드 읽고 분석하기 — Part 2</a>을 읽고 공부한 내용을 정리하는 글입니다.</p>
</blockquote>

<h2 id="모든-중단-함수는-취소-요청에-응답해야-한다">모든 중단 함수는 취소 요청에 응답해야 한다</h2>

<hr />

<p>코루틴에서 실행되는 모든 중단 함수는 취소 요청에 응답하도록 구현되어야 한다. 중단 함수의 실행 중 취소 가능한 구간마다 취소 요청이 있었는지 확인하고, 취소 요청이 있었다면 즉시 실행을 취소하도록 구현되어야 한다. 취소가 가능한 지점마다 현재 코루틴이 취소 여부를 확인하다가 만약 취소되었다면 <code class="language-plaintext highlighter-rouge">CancellationException</code> 을 발생시키며 종료한다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">job</span> <span class="p">=</span> <span class="nf">launch</span><span class="p">(</span><span class="nc">Dispathcers</span><span class="p">.</span><span class="nc">Default</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">for</span> <span class="p">(</span><span class="n">i</span> <span class="k">in</span> <span class="mi">1</span><span class="o">..</span><span class="mi">10</span><span class="p">)</span> <span class="p">{</span>
            <span class="nf">println</span><span class="p">(</span><span class="s">"$i"</span><span class="p">)</span>
            <span class="nc">Thread</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mi">500L</span><span class="p">)</span>
        <span class="p">}</span>
    <span class="p">}</span>

    <span class="nf">delay</span><span class="p">(</span><span class="mi">1300L</span><span class="p">)</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"취소 요청"</span><span class="p">)</span>
    <span class="n">job</span><span class="p">.</span><span class="nf">cancelAndJoin</span><span class="p">()</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"종료"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1
2
3
취소 요청
4
5
6
7
8
9
10
종료
</code></pre></div></div>

<p>위 코드에서는 취소되지 않고 10까지 출력하게 된다. 이 코드를 취소 요청에 친화적인 코드로 만들기 위해서는 취소 가능한 지점마다 Continuation에 실행 시간을 양보하는 <code class="language-plaintext highlighter-rouge">yield()</code> 함수를 호출하거나 CoroutineScope 에 정의된 <code class="language-plaintext highlighter-rouge">isActive</code> 속성을 사용해 코루틴이 비활성 상태인 경우 작업을 중단하도록 작성할 수 있다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">job</span> <span class="p">=</span> <span class="nf">launch</span><span class="p">(</span><span class="nc">Dispathcers</span><span class="p">.</span><span class="nc">Default</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">for</span> <span class="p">(</span><span class="n">i</span> <span class="k">in</span> <span class="mi">1</span><span class="o">..</span><span class="mi">10</span><span class="p">)</span> <span class="p">{</span>
            <span class="k">yield</span><span class="p">()</span> <span class="c1">// &lt;--</span>
            <span class="nf">println</span><span class="p">(</span><span class="s">"$i"</span><span class="p">)</span>
            <span class="nc">Thread</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mi">500L</span><span class="p">)</span>
        <span class="p">}</span>
    <span class="p">}</span>

    <span class="nf">delay</span><span class="p">(</span><span class="mi">1300L</span><span class="p">)</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"취소 요청"</span><span class="p">)</span>
    <span class="n">job</span><span class="p">.</span><span class="nf">cancelAndJoin</span><span class="p">()</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"종료"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">job</span> <span class="p">=</span> <span class="nf">launch</span><span class="p">(</span><span class="nc">Dispathcers</span><span class="p">.</span><span class="nc">Default</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">for</span> <span class="p">(</span><span class="n">i</span> <span class="k">in</span> <span class="mi">1</span><span class="o">..</span><span class="mi">10</span><span class="p">)</span> <span class="p">{</span>
            <span class="k">if</span> <span class="p">(!</span><span class="n">isActive</span><span class="p">)</span> <span class="p">{</span> <span class="c1">// &lt;--</span>
                <span class="k">break</span>
            <span class="p">}</span>
            <span class="nf">println</span><span class="p">(</span><span class="s">"$i"</span><span class="p">)</span>
            <span class="nc">Thread</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mi">500L</span><span class="p">)</span>
        <span class="p">}</span>
    <span class="p">}</span>

    <span class="nf">delay</span><span class="p">(</span><span class="mi">1300L</span><span class="p">)</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"취소 요청"</span><span class="p">)</span>
    <span class="n">job</span><span class="p">.</span><span class="nf">cancelAndJoin</span><span class="p">()</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"종료"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1
2
3
취소 요청
종료
</code></pre></div></div>

<p>두 개의 코드 모두 위와 같이 취소 요청 후 바로 취소할 수 있게 된다.</p>

<p>취소가 되면 <strong>CancellationException</strong>이 발생하는데 이를 처리하기 위해 <code class="language-plaintext highlighter-rouge">try ~ finally</code> 구문이나 Kotlin의 <code class="language-plaintext highlighter-rouge">use()</code> 함수를 사용할 수 있다.</p>

<p><em>try ~ finally</em></p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">job</span> <span class="p">=</span> <span class="nf">launch</span><span class="p">(</span><span class="nc">Dispatchers</span><span class="p">.</span><span class="nc">Default</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">try</span> <span class="p">{</span>
            <span class="k">for</span> <span class="p">(</span><span class="n">i</span> <span class="k">in</span> <span class="mi">1</span><span class="o">..</span><span class="mi">10</span><span class="p">)</span> <span class="p">{</span>
                <span class="nf">println</span><span class="p">(</span><span class="s">"$i"</span><span class="p">)</span>
                <span class="nf">delay</span><span class="p">(</span><span class="mi">500L</span><span class="p">)</span>
            <span class="p">}</span>
        <span class="p">}</span> <span class="k">finally</span> <span class="p">{</span>
            <span class="nf">println</span><span class="p">(</span><span class="s">"실행 완료"</span><span class="p">)</span>
        <span class="p">}</span>

    <span class="p">}</span>

    <span class="nf">delay</span><span class="p">(</span><span class="mi">1300L</span><span class="p">)</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"취소 요청"</span><span class="p">)</span>
    <span class="n">job</span><span class="p">.</span><span class="nf">cancelAndJoin</span><span class="p">()</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"종료"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1
2
3
취소 요청
실행 완료
종료
</code></pre></div></div>

<p><em>use()</em></p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">job</span> <span class="p">=</span> <span class="nf">launch</span><span class="p">(</span><span class="nc">Dispatchers</span><span class="p">.</span><span class="nc">Default</span><span class="p">)</span> <span class="p">{</span>
        <span class="nc">CancellationTest</span><span class="p">().</span><span class="nf">use</span> <span class="p">{</span>
            <span class="n">it</span><span class="p">.</span><span class="nf">repeatedPrint</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>
        <span class="p">}</span>
    <span class="p">}</span>

    <span class="nf">delay</span><span class="p">(</span><span class="mi">1300L</span><span class="p">)</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"취소 요청"</span><span class="p">)</span>
    <span class="n">job</span><span class="p">.</span><span class="nf">cancelAndJoin</span><span class="p">()</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"종료"</span><span class="p">)</span>
<span class="p">}</span>

<span class="kd">class</span> <span class="nc">CancellationTest</span> <span class="p">:</span> <span class="nc">Closeable</span> <span class="p">{</span>

    <span class="k">suspend</span> <span class="k">fun</span> <span class="nf">repeatedPrint</span><span class="p">(</span><span class="n">times</span><span class="p">:</span> <span class="nc">Int</span><span class="p">)</span> <span class="p">{</span>
        <span class="nf">repeat</span><span class="p">(</span><span class="n">times</span><span class="p">)</span> <span class="p">{</span> <span class="n">i</span> <span class="p">-&gt;</span>
            <span class="nf">println</span><span class="p">(</span><span class="s">"$i"</span><span class="p">)</span>
            <span class="nf">delay</span><span class="p">(</span><span class="mi">500L</span><span class="p">)</span>
        <span class="p">}</span>
    <span class="p">}</span>

    <span class="k">override</span> <span class="k">fun</span> <span class="nf">close</span><span class="p">()</span> <span class="p">{</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"CancellationTest - close()"</span><span class="p">)</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0
1
2
취소 요청
CancellationTest - close()
종료
</code></pre></div></div>

<h2 id="취소된-코루틴에서-중단-함수-호출">취소된 코루틴에서 중단 함수 호출</h2>

<hr />

<p>취소된 코루틴의 finally 블록 안에서 중단 함수를 호출하면 <strong>CancellableException</strong>이 발생한다.</p>

<p>보통 리소스를 정리하는 함수들은 Non-Blocking으로 동작하기 때문에 큰 문제가 되지는 않지만 만약 취소된 코루틴 안에서 동기적으로 어떤 중단 함수를 호출해야 하는 경우에는 <code class="language-plaintext highlighter-rouge">withContext { }</code> 코루틴 빌더에 <code class="language-plaintext highlighter-rouge">NonCancellable</code> 컨텍스트를 전달해서 처리할 수 있다.</p>

<blockquote>
  <p>NonCancellable에 관한 내용은 <a href="https://mangbaam.github.io/kotlin/2022/07/15/coroutine-withcontext.html">이 글</a>을 참고하세요.</p>
</blockquote>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">job</span> <span class="p">=</span> <span class="nf">launch</span><span class="p">(</span><span class="nc">Dispatchers</span><span class="p">.</span><span class="nc">Default</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">try</span> <span class="p">{</span>
            <span class="nf">repeat</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span> <span class="p">{</span> <span class="n">i</span> <span class="p">-&gt;</span>
                <span class="nf">println</span><span class="p">(</span><span class="s">"$i"</span><span class="p">)</span>
                <span class="nf">delay</span><span class="p">(</span><span class="mi">500L</span><span class="p">)</span>
            <span class="p">}</span>
        <span class="p">}</span> <span class="k">finally</span> <span class="p">{</span>
            <span class="nf">withContext</span><span class="p">(</span><span class="nc">NonCancellable</span><span class="p">)</span> <span class="p">{</span>
                <span class="nf">delay</span><span class="p">(</span><span class="mi">1000L</span><span class="p">)</span>
                <span class="nf">println</span><span class="p">(</span><span class="s">"실행 완료"</span><span class="p">)</span>
            <span class="p">}</span>
        <span class="p">}</span>

    <span class="p">}</span>

    <span class="nf">delay</span><span class="p">(</span><span class="mi">1300L</span><span class="p">)</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"취소 요청"</span><span class="p">)</span>
    <span class="n">job</span><span class="p">.</span><span class="nf">cancelAndJoin</span><span class="p">()</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"종료"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0
1
2
취소 요청
실행 완료
종료
</code></pre></div></div>

<h2 id="타임-아웃">타임 아웃</h2>

<hr />

<p>코루틴 실행을 취소하는 많은 이유는 수행 시간이 허용할 수 있는 시간보다 길어진 경우이다. 이 경우 코루틴에 Timeout을 지정하고, 이 시간을 넘어서면 해당 작업을 취소하도록 구현할 수 있다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;):</span> <span class="nc">Unit</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">job</span> <span class="p">=</span> <span class="nf">launch</span><span class="p">(</span><span class="nc">Dispatchers</span><span class="p">.</span><span class="nc">Default</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">try</span> <span class="p">{</span>
            <span class="nf">repeat</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span> <span class="p">{</span> <span class="n">i</span> <span class="p">-&gt;</span>
                <span class="nf">println</span><span class="p">(</span><span class="s">"$i"</span><span class="p">)</span>
                <span class="nf">delay</span><span class="p">(</span><span class="mi">500L</span><span class="p">)</span>
            <span class="p">}</span>
        <span class="p">}</span> <span class="k">finally</span> <span class="p">{</span>
            <span class="nf">println</span><span class="p">(</span><span class="s">"실행 완료"</span><span class="p">)</span>
        <span class="p">}</span>
    <span class="p">}</span>

    <span class="nf">launch</span> <span class="p">{</span>
        <span class="nf">delay</span><span class="p">(</span><span class="mi">1300L</span><span class="p">)</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"취소 요청"</span><span class="p">)</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">job</span><span class="p">.</span><span class="n">isActive</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">job</span><span class="p">.</span><span class="nf">cancelAndJoin</span><span class="p">()</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>0
1
2
취소 요청
실행 완료
</code></pre></div></div>

<p>위와 같이 코드를 짜면 예상한 대로 동작하겠지만 별도의 코루틴에서 취소를 처리하는 작업을 매번 해줘야 하기 때문에 좋은 방법은 아니다. 이때 <code class="language-plaintext highlighter-rouge">withTimeout()</code> 함수를 사용할 수 있다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;):</span> <span class="nc">Unit</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="nf">withTimeout</span><span class="p">(</span><span class="mi">1300L</span><span class="p">)</span> <span class="p">{</span>
        <span class="nf">launch</span> <span class="p">{</span>
            <span class="k">try</span> <span class="p">{</span>
                <span class="nf">repeat</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span> <span class="p">{</span> <span class="n">i</span> <span class="p">-&gt;</span>
                    <span class="nf">println</span><span class="p">(</span><span class="s">"$i"</span><span class="p">)</span>
                    <span class="nf">delay</span><span class="p">(</span><span class="mi">500L</span><span class="p">)</span>
                <span class="p">}</span>
            <span class="p">}</span> <span class="k">finally</span> <span class="p">{</span>
                <span class="nf">println</span><span class="p">(</span><span class="s">"실행 완료"</span><span class="p">)</span>
            <span class="p">}</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><img src="https://user-images.githubusercontent.com/44221447/179161265-5ef9b688-1654-4fc5-9cf6-0c76718276ef.png" alt="timeout" /></p>

<p>TimeoutCancellationException이 발생했는데 이는 메인 함수에서 바로 실행됐기 때문이다.</p>]]></content><author><name>mangbaam</name></author><category term="Kotlin" /><category term="kotlin" /><category term="coroutine" /><summary type="html"><![CDATA[⭐]]></summary></entry><entry><title type="html">Coroutine withContext + NonCancellable</title><link href="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/15/coroutine-withcontext.html" rel="alternate" type="text/html" title="Coroutine withContext + NonCancellable" /><published>2022-07-15T00:00:00+00:00</published><updated>2022-07-15T00:00:00+00:00</updated><id>https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/15/coroutine-withcontext</id><content type="html" xml:base="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/15/coroutine-withcontext.html"><![CDATA[<h2>⭐</h2>

<blockquote>
  <p>본 글은 <a href="https://myungpyo.medium.com/%EC%BD%94%EB%A3%A8%ED%8B%B4-%EA%B3%B5%EC%8B%9D-%EA%B0%80%EC%9D%B4%EB%93%9C-%EC%9E%90%EC%84%B8%ED%9E%88-%EC%9D%BD%EA%B8%B0-part-2-dive-1-4c468828319">withContext 코루틴 빌더가 하는 일은 무엇인가?</a>을 읽고 공부한 내용을 정리하는 글입니다.</p>
</blockquote>

<h2 id="지난-내용">지난 내용</h2>

<p><a href="https://mangbaam.github.io/kotlin/2022/07/15/coroutine-cancellation.html">이전 글</a>에서 중단된 코루틴의 <code class="language-plaintext highlighter-rouge">finally { }</code> 블록에서 중단 함수를 사용할 때 withContext를 사용하는 것에 대해 다뤘다. 간단히 살펴보면 다음과 같다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;):</span> <span class="nc">Unit</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">job</span> <span class="p">=</span> <span class="nf">launch</span> <span class="p">{</span>
        <span class="k">try</span> <span class="p">{</span>
            <span class="nf">repeat</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span> <span class="p">{</span> <span class="n">i</span> <span class="p">-&gt;</span>
                <span class="nf">println</span><span class="p">(</span><span class="s">"$i"</span><span class="p">)</span>
                <span class="nf">delay</span><span class="p">(</span><span class="mi">500L</span><span class="p">)</span>
            <span class="p">}</span>
        <span class="p">}</span> <span class="k">finally</span> <span class="p">{</span>
            <span class="nf">withContext</span><span class="p">(</span><span class="nc">NonCancellable</span><span class="p">)</span> <span class="p">{</span>
                <span class="nf">delay</span><span class="p">(</span><span class="mi">1000L</span><span class="p">)</span> <span class="c1">// &lt;-- 중단 함수</span>
                <span class="nf">println</span><span class="p">(</span><span class="s">"실행 완료"</span><span class="p">)</span>
            <span class="p">}</span>
        <span class="p">}</span>
    <span class="p">}</span>

    <span class="nf">delay</span><span class="p">(</span><span class="mi">1300L</span><span class="p">)</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"중단 시도"</span><span class="p">)</span>
    <span class="n">job</span><span class="p">.</span><span class="nf">cancelAndJoin</span><span class="p">()</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"종료"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">cancelAndJoin()</code>으로 job이 취소되면 try { } 블록에서 CancelledException이 발생하면서 finally { } 블럭으로 진입하게 된다. 이때 withContext 없이 중단 함수인 delay()를 호출하면 현재 코루틴 컨텍스트는 이미 취소된 상태이기 때문에 CancelledException이 다시 발생한다.</p>

<p>이를 withContext { } 와 NonCancellable 컨텍스트 요소의 조합으로 해결할 수 있었다.</p>

<h2 id="withcontext의-반환">withContext의 반환</h2>

<hr />

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">public</span> <span class="k">suspend</span> <span class="k">fun</span> <span class="p">&lt;</span><span class="nc">T</span><span class="p">&gt;</span> <span class="nf">withContext</span><span class="p">(</span>
    <span class="n">context</span><span class="p">:</span> <span class="nc">CoroutineContext</span><span class="p">,</span>
    <span class="n">block</span><span class="p">:</span> <span class="k">suspend</span> <span class="nc">CoroutineScope</span><span class="p">.()</span> <span class="p">-&gt;</span> <span class="nc">T</span>
<span class="p">):</span> <span class="nc">T</span> <span class="p">{</span>
    <span class="nf">contract</span> <span class="p">{</span>
        <span class="nf">callsInPlace</span><span class="p">(</span><span class="n">block</span><span class="p">,</span> <span class="nc">InvocationKind</span><span class="p">.</span><span class="nc">EXACTLY_ONCE</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">suspendCoroutineUninterceptedOrReturn</span> <span class="nd">sc@</span> <span class="p">{</span> <span class="n">uCont</span> <span class="p">-&gt;</span>
        <span class="c1">// compute new context</span>
        <span class="kd">val</span> <span class="py">oldContext</span> <span class="p">=</span> <span class="n">uCont</span><span class="p">.</span><span class="n">context</span>
        <span class="kd">val</span> <span class="py">newContext</span> <span class="p">=</span> <span class="n">oldContext</span> <span class="p">+</span> <span class="n">context</span>
        <span class="c1">// always check for cancellation of new context</span>
        <span class="n">newContext</span><span class="p">.</span><span class="nf">ensureActive</span><span class="p">()</span>
        <span class="c1">// FAST PATH #1 -- new context is the same as the old one</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">newContext</span> <span class="p">===</span> <span class="n">oldContext</span><span class="p">)</span> <span class="p">{</span>
            <span class="kd">val</span> <span class="py">coroutine</span> <span class="p">=</span> <span class="nc">ScopeCoroutine</span><span class="p">(</span><span class="n">newContext</span><span class="p">,</span> <span class="n">uCont</span><span class="p">)</span>
            <span class="k">return</span><span class="nd">@sc</span> <span class="n">coroutine</span><span class="p">.</span><span class="nf">startUndispatchedOrReturn</span><span class="p">(</span><span class="n">coroutine</span><span class="p">,</span> <span class="n">block</span><span class="p">)</span>
        <span class="p">}</span>
        <span class="c1">// FAST PATH #2 -- the new dispatcher is the same as the old one (something else changed)</span>
        <span class="c1">// `equals` is used by design (see equals implementation is wrapper context like ExecutorCoroutineDispatcher)</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">newContext</span><span class="p">[</span><span class="nc">ContinuationInterceptor</span><span class="p">]</span> <span class="p">==</span> <span class="n">oldContext</span><span class="p">[</span><span class="nc">ContinuationInterceptor</span><span class="p">])</span> <span class="p">{</span>
            <span class="kd">val</span> <span class="py">coroutine</span> <span class="p">=</span> <span class="nc">UndispatchedCoroutine</span><span class="p">(</span><span class="n">newContext</span><span class="p">,</span> <span class="n">uCont</span><span class="p">)</span>
            <span class="c1">// There are changes in the context, so this thread needs to be updated</span>
            <span class="nf">withCoroutineContext</span><span class="p">(</span><span class="n">newContext</span><span class="p">,</span> <span class="k">null</span><span class="p">)</span> <span class="p">{</span>
                <span class="k">return</span><span class="nd">@sc</span> <span class="n">coroutine</span><span class="p">.</span><span class="nf">startUndispatchedOrReturn</span><span class="p">(</span><span class="n">coroutine</span><span class="p">,</span> <span class="n">block</span><span class="p">)</span>
            <span class="p">}</span>
        <span class="p">}</span>
        <span class="c1">// SLOW PATH -- use new dispatcher</span>
        <span class="kd">val</span> <span class="py">coroutine</span> <span class="p">=</span> <span class="nc">DispatchedCoroutine</span><span class="p">(</span><span class="n">newContext</span><span class="p">,</span> <span class="n">uCont</span><span class="p">)</span>
        <span class="n">block</span><span class="p">.</span><span class="nf">startCoroutineCancellable</span><span class="p">(</span><span class="n">coroutine</span><span class="p">,</span> <span class="n">coroutine</span><span class="p">)</span>
        <span class="n">coroutine</span><span class="p">.</span><span class="nf">getResult</span><span class="p">()</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>withContext 함수는 로직이 담긴 코드 블록과 이것이 실행될 코루틴 컨텍스트를 매개 변수로 받는다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="err">@</span><span class="n">kotlin</span><span class="p">.</span><span class="nc">SinceKotlin</span> <span class="err">@</span><span class="n">kotlin</span><span class="p">.</span><span class="k">internal</span><span class="p">.</span><span class="nc">InlineOnly</span> <span class="k">public</span> <span class="k">suspend</span> <span class="k">inline</span> <span class="k">fun</span> <span class="p">&lt;</span><span class="nc">T</span><span class="p">&gt;</span> <span class="nf">suspendCoroutineUninterceptedOrReturn</span><span class="p">(</span><span class="k">crossinline</span> <span class="n">block</span><span class="p">:</span> <span class="p">(</span><span class="n">kotlin</span><span class="p">.</span><span class="n">coroutines</span><span class="p">.</span><span class="nc">Continuation</span><span class="p">&lt;</span><span class="nc">T</span><span class="p">&gt;)</span> <span class="p">-&gt;</span> <span class="n">kotlin</span><span class="p">.</span><span class="nc">Any</span><span class="p">?):</span> <span class="nc">T</span> <span class="p">{</span> <span class="nf">contract</span> <span class="p">{</span> <span class="cm">/* compiled contract */ }; /* compiled code */</span> <span class="p">}</span>
</code></pre></div></div>

<p>그리고 <a href="https://kotlinlang.org/api/latest/jvm/stdlib/kotlin.coroutines.intrinsics/suspend-coroutine-unintercepted-or-return.html">suspendCoroutineUninterceptedOrReturn()</a> 함수를 반환하는데 이것은 전달된 코드 블럭에서 호출 코루틴(Continuation) 정보에 접근할 수 있도록 해준다. 전달된 코드 블록에서 COROUTINE_SUSPENDED 라는 사전 정의 된 상수 값을 반환 할 경우 코루틴이 처리를 위해 시간이 필요하며 즉시 값을 반환하지 않고 처리가 완료되면 continuation 파라미터를 통해 결과를 전달할 것을 나타내며, 그 이외의 값을 반환할 경우 중단 없이 바로 결과 값을 반환할 것을 나타낸다.</p>

<h2 id="withcontext-3가지-방식">withContext 3가지 방식</h2>

<hr />

<p>withContext 함수로 전달된 컨텍스트에 따라 3가지 방식으로 처리가 된다.</p>

<ul>
  <li>Function Caller - Callee 코루틴 컨텍스트가 완전히 동일한 경우 (===)
    <ul>
      <li>현재 컨텍스트에서 그대로 코드 블럭을 실행해도 무방하기 때문에 ScopeCoroutine을 만들어 바로 실행한다. ScopeCoroutine은 코루틴 간 컨텍스트가 동일해서 바로 작업을 수행할 수 있을 경우 사용됨</li>
    </ul>
  </li>
  <li>Function Caller - Callee 코루틴 컨텍스트가 서로 다른 부분이 있지만 Dispatcher 컨텍스트 요소는 동일한 경우 (==)
    <ul>
      <li>UndispatchedCoroutine을 만들고 새로 만들어진 컨텍스트 안에서 실행 (withContext)</li>
    </ul>
  </li>
  <li>그 외의 경우
    <ul>
      <li>코루틴 컨텍스트 간에 dispatcher를 포함한 변경 사항이 있는 것이기 때문에 새로운 컨텍스트를 이용해 적절한 스레드로 dispatch 되어 실행될 수 있도록 DispathcedCoroutine을 생성해서 실행</li>
    </ul>
  </li>
</ul>

<h2 id="noncancellable">NonCancellable</h2>

<hr />

<p>지난 내용에서 NonCancellable을 withContext로 넘기는 부분이 있었다. NonCancellable은 어떻게 취소된 코루틴 블록에서 예외를 발생시키지 않고 작업을 이어나갈 수 있을까?</p>

<p>내부를 살펴보자.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">public</span> <span class="kd">object</span> <span class="nc">NonCancellable</span> <span class="p">:</span> <span class="nc">AbstractCoroutineContextElement</span><span class="p">(</span><span class="nc">Job</span><span class="p">),</span> <span class="nc">Job</span> <span class="p">{</span>

    <span class="k">private</span> <span class="k">const</span> <span class="kd">val</span> <span class="py">message</span> <span class="p">=</span> <span class="s">"NonCancellable can be used only as an argument for 'withContext', direct usages of its API are prohibited"</span>

    <span class="cm">/**
     * Always returns `true`.
     * @suppress **This an internal API and should not be used from general code.**
     */</span>
    <span class="nd">@Deprecated</span><span class="p">(</span><span class="n">level</span> <span class="p">=</span> <span class="nc">DeprecationLevel</span><span class="p">.</span><span class="nc">WARNING</span><span class="p">,</span> <span class="n">message</span> <span class="p">=</span> <span class="n">message</span><span class="p">)</span>
    <span class="k">override</span> <span class="kd">val</span> <span class="py">isActive</span><span class="p">:</span> <span class="nc">Boolean</span>
        <span class="k">get</span><span class="p">()</span> <span class="p">=</span> <span class="k">true</span>

    <span class="cm">/**
     * Always returns `false`.
     * @suppress **This an internal API and should not be used from general code.**
     */</span>
    <span class="nd">@Deprecated</span><span class="p">(</span><span class="n">level</span> <span class="p">=</span> <span class="nc">DeprecationLevel</span><span class="p">.</span><span class="nc">WARNING</span><span class="p">,</span> <span class="n">message</span> <span class="p">=</span> <span class="n">message</span><span class="p">)</span>
    <span class="k">override</span> <span class="kd">val</span> <span class="py">isCompleted</span><span class="p">:</span> <span class="nc">Boolean</span> <span class="k">get</span><span class="p">()</span> <span class="p">=</span> <span class="k">false</span>

    <span class="cm">/**
     * Always returns `false`.
     * @suppress **This an internal API and should not be used from general code.**
     */</span>
    <span class="nd">@Deprecated</span><span class="p">(</span><span class="n">level</span> <span class="p">=</span> <span class="nc">DeprecationLevel</span><span class="p">.</span><span class="nc">WARNING</span><span class="p">,</span> <span class="n">message</span> <span class="p">=</span> <span class="n">message</span><span class="p">)</span>
    <span class="k">override</span> <span class="kd">val</span> <span class="py">isCancelled</span><span class="p">:</span> <span class="nc">Boolean</span> <span class="k">get</span><span class="p">()</span> <span class="p">=</span> <span class="k">false</span>

    <span class="c1">// ...</span>
</code></pre></div></div>

<p>NonCancellable은 AbstractCoroutineContextElement를 상속하는 컨텍스트 요소이다. 작업을 이어나갈 수 있는 이유는 isActive가 항상 true를 반환하기 때문이다.</p>

<p>그렇다면 withContext(NonCancellable)은 <a href="#withcontext-3가지-방식">withContext 3가지 방식</a> 중 어느 방식으로 진행될까?</p>

<p>정답은 두 번째 경로이다.</p>

<p>코드에서 oldContext는 현재 코루틴의 컨텍스트이고, newContext는 oldContext + NonCancellable 이다. NonCancellable에는 dispatcher 컨텍스트 요소에 대한 정의가 들어있지 않으므로 둘을 plus 하게 되면 oldContext의 dispatcher 요소를 상속하여 사용하게 된다.</p>

<blockquote>
  <p>NonCancellable은 withContext 에서만 사용되어야 한다.</p>
</blockquote>]]></content><author><name>mangbaam</name></author><category term="Kotlin" /><category term="kotlin" /><category term="coroutine" /><summary type="html"><![CDATA[⭐]]></summary></entry><entry><title type="html">AutoHotKey를 사용해 완전 편하게 개발자 키매핑 하기</title><link href="https://mangbaam.github.io//jekyll-theme-yat/tip/2022/07/11/tip-auto-hot-key.html" rel="alternate" type="text/html" title="AutoHotKey를 사용해 완전 편하게 개발자 키매핑 하기" /><published>2022-07-11T00:00:00+00:00</published><updated>2022-07-11T00:00:00+00:00</updated><id>https://mangbaam.github.io//jekyll-theme-yat/tip/2022/07/11/tip-auto-hot-key</id><content type="html" xml:base="https://mangbaam.github.io//jekyll-theme-yat/tip/2022/07/11/tip-auto-hot-key.html"><![CDATA[<h2>⭐</h2>

<p>본 게시글에서 공유한 스크립트는 언제든 변경되거나 추가될 수 있습니다!</p>

<p><a href="https://github.com/mangbaam/AutoHotKey">깃허브</a>에서 최신의 내용을 확인할 수 있습니다. 복사해서 자유롭게 사용하세요!</p>

<p>더 좋은 방법이나 버그, 추가하면 좋을 키매핑 등 자유롭게 의견 주세요!</p>

<p><a href="#완료된-모습">완성된 키매핑</a>을 바로 보고싶다면 클릭하세요.</p>

<h2 id="극한의-효율성">극한의 효율성</h2>

<hr />

<p><img src="https://user-images.githubusercontent.com/44221447/178151444-dd6314ca-6c10-4d5c-9cfb-f823c5719258.png" alt="annepro2" /></p>

<p>나는 <strong>Anne Pro2</strong> 키보드를 2년 간 사용하고 더 좋은 키감을 가진 키보드를 가지고 싶다는 생각으로 <strong>Durgod K320KR</strong> 저소음 적축 모델을 구매하게 되었다.</p>

<p>키감은 정말 만족스러웠다. 하지만 포커 배열(60% 배열)인 앤프로2를 사용할 때 두 손이 키보드를 절대 벗어나지 않도록 (개인적으로) 완벽한 키매핑을 했기에 텐키리스 키보드에서 방향키나 <code class="language-plaintext highlighter-rouge">HOME</code>, <code class="language-plaintext highlighter-rouge">END</code> 등의 키를 누르기 위해 손이 자리에서 벗어나서 오른쪽으로 이동해야 한다는 점이 너무나도 불편하게 느껴졌다.</p>

<p>물론 한 달 정도 현재 키보드로만 타이핑을 하다보니 생각보다 금방 익숙해졌지만 이미 극한의 효율을 맛 본 이후라 만족스럽지는 못했다.</p>

<p><img src="https://user-images.githubusercontent.com/44221447/178151664-b1aaea74-ae9f-46c3-9e77-3932fcb6e800.png" alt="powertoys" /></p>

<p>처음에는 <strong>PowerToys</strong>의 키매핑 기능으로 비교적 덜 사용되는 <code class="language-plaintext highlighter-rouge">Alt</code> 키를 가지고 조합을 시도했다. 참고로 앤프로2는 <code class="language-plaintext highlighter-rouge">CapsLock</code> 버튼을 <code class="language-plaintext highlighter-rouge">Fn키</code>로 사용하기 때문에 엄청난 효율을 챙길 수 있는 것이었는데, 키보드에 있는 <code class="language-plaintext highlighter-rouge">Fn키</code>는 공식 키매핑 프로그램이나 딥 스위치로 제공하지 않으면 옮기기 힘들고, <code class="language-plaintext highlighter-rouge">CapsLock</code>은 <code class="language-plaintext highlighter-rouge">Ctrl</code>, <code class="language-plaintext highlighter-rouge">Alt</code>, <code class="language-plaintext highlighter-rouge">Shift</code>와 같은 조합 키가 아니기 때문에 <strong>PowerToys</strong>의 기능으로는 <code class="language-plaintext highlighter-rouge">CapsLock</code> 위치에 <code class="language-plaintext highlighter-rouge">Alt</code> 키를 매핑한 후 <code class="language-plaintext highlighter-rouge">Alt</code>와 다른 키들의 조합으로 하는 것이 최선이었다.</p>

<p>하지만 나는 단축키를 사용하는 것을 즐기기에 생각보다 Alt 키를 사용하는 경우도 많이 있었고, 이때마다 조합해두었던 키들과 충돌을 일으켜 원하는 기능을 제대로 사용할 수 없는 경우가 허다했다.</p>

<p>그렇게 불편함을 감수하고 있다가 오늘 우연히 <strong>Auto Hot Key</strong> 라는 프로그램을 알게되어 기존의 앤프로2 키매핑과 거의 유사하게 스크립트를 짜서 적용해볼 수 있었다.</p>

<p>다른 사람들도 꼭 키보드에서 손이 벗어나지 않아도 되는 <strong>극한의 효율성</strong>을 느끼게 해주고 싶어서 스크립트와 방법을 공유하고자 한다.</p>

<p><em>앤프로 후기 및 키매핑 -&gt; <a href="https://latte-is-horse.tistory.com/89">https://latte-is-horse.tistory.com/89</a></em></p>

<h2 id="설치">설치</h2>

<hr />

<p>AutoHotKey를 사용하려면 우선 설치를 해야한다.</p>

<p><a href="https://www.autohotkey.com/download/">https://www.autohotkey.com/download/</a></p>

<p>위 공식 링크에서 설치 파일을 다운로드 할 수 있다.</p>

<p><img src="https://user-images.githubusercontent.com/44221447/178151925-5261a38a-cb68-422c-a687-aba429789b62.png" width="50%" /></p>

<p>Express Installation 밑에 있는 경로에 그대로 설치하려면 Express Installation을, 다른 경로에 설치하려면 Custom Installation을 선택한다.</p>

<p><img src="https://user-images.githubusercontent.com/44221447/178151968-4994f6a2-fc87-44c3-8b10-e50c0dc97eb3.png" width="50%" /></p>

<p>Exit을 누르고 설치를 종료한다.</p>

<h2 id="vs-code-설정">VS Code 설정</h2>

<hr />

<p>AutoHotKey는 일종의 스크립트인데 메모장으로도 편집이 가능하지만 개발자라면 대부분 설치되어 있을 VS Code에 설정하고 작성하는 방법을 공유하고자 한다.</p>

<p><img src="https://user-images.githubusercontent.com/44221447/178152038-802ad79a-b4ba-4779-99c9-4dc3cb43a405.png" alt="vscode" /></p>

<p>VS Code의 확장 프로그램에서 검색해보면 AutoHotKey 확장 프로그램이 있다. <code class="language-plaintext highlighter-rouge">설치</code>한다.</p>

<p><img src="https://user-images.githubusercontent.com/44221447/178152095-1d8eb400-b509-46c4-a0e3-a47542d79f16.png" alt="코드러너" /></p>

<p>아마 VS Code를 사용한다면 Code Runner는 대부분 설치되어 있을 것 같은데 Code Runner에서도 <code class="language-plaintext highlighter-rouge">.ahk</code> 확장자를 지원한다. 이 확장자가 AutoHotKey 파일의 확장자이다.</p>

<p><em>settings.json</em></p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
    </span><span class="nl">"code-runner.executorMap"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="err">//</span><span class="w"> </span><span class="err">...</span><span class="w">
        </span><span class="nl">"ahk"</span><span class="p">:</span><span class="w"> </span><span class="s2">"</span><span class="se">\"</span><span class="s2">C:</span><span class="se">\\</span><span class="s2">Program Files</span><span class="se">\\</span><span class="s2">AutoHotkey</span><span class="se">\\</span><span class="s2">AutoHotkey.exe</span><span class="se">\"</span><span class="s2">"</span><span class="p">,</span><span class="w">
        </span><span class="err">//</span><span class="w"> </span><span class="err">...</span><span class="w">
    </span><span class="p">},</span><span class="w">
    </span><span class="nl">"code-runner.executorMapByFileExtension"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
		</span><span class="err">//</span><span class="w"> </span><span class="err">...</span><span class="w">
        </span><span class="nl">".ahk"</span><span class="p">:</span><span class="w"> </span><span class="s2">"</span><span class="se">\"</span><span class="s2">C:</span><span class="se">\\</span><span class="s2">Program Files</span><span class="se">\\</span><span class="s2">AutoHotkey</span><span class="se">\\</span><span class="s2">AutoHotkey.exe</span><span class="se">\"</span><span class="s2">"</span><span class="p">,</span><span class="w">
        </span><span class="err">//</span><span class="w"> </span><span class="err">...</span><span class="w">
    </span><span class="p">},</span><span class="w">
	</span><span class="err">//</span><span class="w"> </span><span class="err">...</span><span class="w">
    </span><span class="nl">"[ahk]"</span><span class="p">:</span><span class="w"> 
    </span><span class="p">{</span><span class="w">
        </span><span class="nl">"files.encoding"</span><span class="p">:</span><span class="w"> </span><span class="s2">"utf8"</span><span class="p">,</span><span class="w">
    </span><span class="p">},</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">F1</code> 이나 <code class="language-plaintext highlighter-rouge">Ctrl + p</code>를 눌러 <code class="language-plaintext highlighter-rouge">settings.json</code>을 열고 위와 같이 추가한다.</p>

<p><img src="https://user-images.githubusercontent.com/44221447/178152298-18440981-ab07-445d-9e47-817879e22643.png" alt="경로" /></p>

<p><code class="language-plaintext highlighter-rouge">ahk</code>에 추가되는 경로는 실제 경로를 선택하자.</p>

<p><em>keybindings.json</em></p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">[</span><span class="w">
	</span><span class="err">//</span><span class="w"> </span><span class="err">...</span><span class="w">
	</span><span class="p">{</span><span class="w"> </span><span class="nl">"key"</span><span class="p">:</span><span class="w"> </span><span class="s2">"f5"</span><span class="p">,</span><span class="w"> </span><span class="nl">"command"</span><span class="p">:</span><span class="w"> </span><span class="s2">"code-runner.run"</span><span class="p">,</span><span class="nl">"when"</span><span class="p">:</span><span class="w"> </span><span class="s2">"editorTextFocus &amp;&amp; editorLangId == 'ahk'"</span><span class="p">},</span><span class="w">
	</span><span class="p">{</span><span class="w"> </span><span class="nl">"key"</span><span class="p">:</span><span class="w"> </span><span class="s2">"f6"</span><span class="p">,</span><span class="w"> </span><span class="nl">"command"</span><span class="p">:</span><span class="w"> </span><span class="s2">"code-runner.stop"</span><span class="w"> </span><span class="p">},</span><span class="w">
</span><span class="p">]</span><span class="w">
</span></code></pre></div></div>

<p>마찬가지로 Open Keyboard Shortcuts (JSON)에서 위 코드를 추가한다. <code class="language-plaintext highlighter-rouge">ahk</code> 파일에서 <code class="language-plaintext highlighter-rouge">F5</code>를 누르면 실행되도록 하는 단축키를 추가하는 부분이다.</p>

<p><img src="https://user-images.githubusercontent.com/44221447/178152373-c0ea2d30-4998-414a-a88a-2bcee236117a.png" alt="트레이" /></p>

<p>저장하고 <code class="language-plaintext highlighter-rouge">ahk</code> 파일에서 <code class="language-plaintext highlighter-rouge">F5</code> 키를 눌러보면 위와 같이 트레이에 추가되어 실행되는 것을 확인할 수 있다.</p>

<h2 id="키매핑">키매핑</h2>

<hr />

<p><img src="https://user-images.githubusercontent.com/44221447/178152449-2e64fd3b-4803-4b77-b2e0-04f911a45f35.png" alt="앤프로" /></p>

<p>목표는 기존의 앤프로와 유사하게 만드는 것이다.</p>

<h3 id="capslock을-fn키로-사용하기">CapsLock을 Fn키로 사용하기</h3>

<pre><code class="language-ahk">*CapsLock:: fn:=1
*CapsLock Up:: fn:=0
LWin &amp; Space:: CapsLock ; 캡스락 대체키

#if fn
; 키매핑
</code></pre>

<p><code class="language-plaintext highlighter-rouge">CapsLock</code>에 <code class="language-plaintext highlighter-rouge">fn</code> 이라는 변수를 할당했고, 누르면 1, 떼면 0으로 토글되도록 하였다.</p>

<p>가끔 캡스락을 사용하는 경우가 있기 때문에 <code class="language-plaintext highlighter-rouge">Win + Space</code> 키로 캡스락 기능을 하도록 만들었다.</p>

<p><code class="language-plaintext highlighter-rouge">#if fn</code> 밑으로 키매핑을 하면 캡스락이 눌린 상태에서 동작하는 키를 만들 수 있다.</p>

<h3 id="방향키">방향키</h3>

<p><img src="https://user-images.githubusercontent.com/44221447/178152533-4a9b86a8-a3ab-465a-8479-baadb78ed57f.png" alt="방향키" /></p>

<pre><code class="language-ahk">;***************************************
;   캡스락 설정
;***************************************

*CapsLock:: fn:=1
*CapsLock Up:: fn:=0
LWin &amp; Space:: CapsLock ; 캡스락 대체키

;***************************************
;   방향키 설정
;***************************************
; 기본 WASD, IJKL
#if fn
j::Send, {Left} ; LEFT
a::Send, {Left}
k::Send, {Down} ; DOWN
s::Send, {Down}
l::Send, {Right} ; RIGHT
d::Send, {Right}
i::Send, {Up} ; UP
w::Send, {Up}

; Shift 조합
+j::Send, {ShiftDown}{Left} ; LEFT
+a::Send, {ShiftDown}{Left}
+k::Send, {ShiftDown}{Down} ; DOWN
+s::Send, {ShiftDown}{Down}
+l::Send, {ShiftDown}{Right} ; RIGHT
+d::Send, {ShiftDown}{Right}
+w::Send, {ShiftDown}{Up}
+i::Send, {ShiftDown}{Up} ; UP

; Ctrl 조합
^j::Send, {CtrlDown}{Left} ; LEFT
^a::Send, {CtrlDown}{Left}
^k::Send, {CtrlDown}{Down} ; DOWN
^s::Send, {CtrlDown}{Down}
^l::Send, {CtrlDown}{Right} ; RIGHT
^d::Send, {CtrlDown}{Right}
^i::Send, {CtrlDown}{Up} ; UP
^w::Send, {CtrlDown}{Up}

; Alt 조합
!j::Send, {AltDown}{Left} ; LEFT
!a::Send, {AltDown}{Left}
!k::Send, {AltDown}{Down} ; DOWN
!s::Send, {AltDown}{Down}
!l::Send, {AltDown}{Right} ; RIGHT
!d::Send, {AltDown}{Right}
!i::Send, {AltDown}{Up} ; UP
!w::Send, {AltDown}{Up}

; Ctrl Shift 조합
^+j::Send, {CtrlDown}{ShiftDown}{Left} ; LEFT
^+a::Send, {CtrlDown}{ShiftDown}{Left}
^+k::Send, {CtrlDown}{ShiftDown}{Down} ; DOWN
^+s::Send, {CtrlDown}{ShiftDown}{Down}
^+l::Send, {CtrlDown}{ShiftDown}{Right} ; RIGHT
^+d::Send, {CtrlDown}{ShiftDown}{Right}
^+i::Send, {CtrlDown}{ShiftDown}{Up} ; UP
^+w::Send, {CtrlDown}{ShiftDown}{Up}
</code></pre>

<p>위 사진과 같이 방향키를 설정했다. 방향키는 조합키과 사용하는 경우가 많아서 주로 사용되는 조합을 추가했다.</p>

<blockquote>
  <p>혹시 이렇게 일일이 지정하지 않아도 되는 방법이 있다면 알려주세요!!</p>
</blockquote>

<h3 id="f-키">F 키</h3>

<p><img src="https://user-images.githubusercontent.com/44221447/178152825-f793b700-4341-49fe-96c1-edec6feb89d8.png" alt="f키" /></p>

<pre><code class="language-ahk">;***************************************
;   캡스락 설정
;***************************************
*CapsLock:: fn:=1
*CapsLock Up:: fn:=0
LWin &amp; Space:: CapsLock ; 캡스락 대체키

;***************************************
;   F키 설정
;***************************************
#if fn
1::Send, {F1}
2::Send, {F2}
3::Send, {F3}
4::Send, {F4}
5::Send, {F5}
6::Send, {F6}
7::Send, {F7}
8::Send, {F8}
9::Send, {F9}
0::Send, {F10}
-::Send, {F11}
=::Send, {F12}

; Shift 조합
+1::Send, {ShiftDown}{F1}
+2::Send, {ShiftDown}{F2}
+3::Send, {ShiftDown}{F3}
+4::Send, {ShiftDown}{F4}
+5::Send, {ShiftDown}{F5}
+6::Send, {ShiftDown}{F6}
+7::Send, {ShiftDown}{F7}
+8::Send, {ShiftDown}{F8}
+9::Send, {ShiftDown}{F9}
+0::Send, {ShiftDown}{F10}
+-::Send, {ShiftDown}{F11}
+=::Send, {ShiftDown}{F12}

; Ctrl 조합
^1::Send, {CtrlDown}{F1}
^2::Send, {CtrlDown}{F2}
^3::Send, {CtrlDown}{F3}
^4::Send, {CtrlDown}{F4}
^5::Send, {CtrlDown}{F5}
^6::Send, {CtrlDown}{F6}
^7::Send, {CtrlDown}{F7}
^8::Send, {CtrlDown}{F8}
^9::Send, {CtrlDown}{F9}
^0::Send, {CtrlDown}{F10}
^-::Send, {CtrlDown}{F11}
^=::Send, {CtrlDown}{F12}

; Ctrl + Shift 조합
^+1::Send, {CtrlDown}{ShiftDown}{F1}
^+2::Send, {CtrlDown}{ShiftDown}{F2}
^+3::Send, {CtrlDown}{ShiftDown}{F3}
^+4::Send, {CtrlDown}{ShiftDown}{F4}
^+5::Send, {CtrlDown}{ShiftDown}{F5}
^+6::Send, {CtrlDown}{ShiftDown}{F6}
^+7::Send, {CtrlDown}{ShiftDown}{F7}
^+8::Send, {CtrlDown}{ShiftDown}{F8}
^+9::Send, {CtrlDown}{ShiftDown}{F9}
^+0::Send, {CtrlDown}{ShiftDown}{F10}
^+-::Send, {CtrlDown}{ShiftDown}{F11}
^+=::Send, {CtrlDown}{ShiftDown}{F12}

; Alt 조합
!1::Send, {AltDown}{F1}
!2::Send, {AltDown}{F2}
!3::Send, {AltDown}{F3}
!4::Send, {AltDown}{F4}
!5::Send, {AltDown}{F5}
!6::Send, {AltDown}{F6}
!7::Send, {AltDown}{F7}
!8::Send, {AltDown}{F8}
!9::Send, {AltDown}{F9}
!0::Send, {AltDown}{F10}
!-::Send, {AltDown}{F11}
!=::Send, {AltDown}{F12}
</code></pre>

<h3 id="home-end-pageup-pagedown-insert-delete">Home, End, PageUp, PageDown, Insert, Delete</h3>

<p><img src="https://user-images.githubusercontent.com/44221447/178152937-848af9d4-fdab-41ad-b7d8-02d1e6e0037a.png" alt="기능키" /></p>

<p>코딩할 때 정말 유용하게 사용되는 키들이다.</p>

<pre><code class="language-ahk">;***************************************
;   캡스락 설정
;***************************************
*CapsLock:: fn:=1
*CapsLock Up:: fn:=0
LWin &amp; Space:: CapsLock ; 캡스락 대체키

;***************************************
;   HOME, END, PGUP, PGDN 설정
;***************************************
#if fn
`;::Send, {Home} ; HOME
'::Send, {End} ; END
q::Send, {PgUp} ; PageUp
e::Send, {PgDn} ; PageDown

; Shift 조합
+`;::Send, {ShiftDown}{Home} ; HOME
+'::Send, {ShiftDown}{End} ; END
+q::Send, {ShiftDown}{PgUp} ; PageUp
+e::Send, {ShiftDown}{PgDn} ; PageDown

; Alt 조합
!`;::Send, {AltDown}{Home} ; HOME
!'::Send, {AltDown}{End} ; END
!q::Send, {AltDown}{PgUp} ; PageUp
!e::Send, {AltDown}{PgDn} ; PageDown

;***************************************
;   Insert, Delete 설정
;***************************************
.::Send, {Insert} ; INSERT
/::Send, {Delete} ; DELETE

; Alt 조합
!.::Send, {AltDown}{Insert} ; INSERT
!/::Send, {AltDown}{Delete} ; DELETE

; Shift 조합
+.::Send, {ShiftDown}{Insert} ; INSERT
+/::Send, {ShiftDown}{Delete} ; DELETE

; Ctrl 조합
^.::Send, {CtrlDown}{Insert} ; INSERT
^/::Send, {CtrlDown}{Delete} ; DELETE

; Ctrl + Alt 조합
^!.::Send, {CtrlDown}{AltDown}{Insert} ; INSERT
^!/::Send, {CtrlDown}{AltDown}{Delete} ; DELETE
</code></pre>

<h3 id="멀티미디어-키">멀티미디어 키</h3>

<p><img src="https://user-images.githubusercontent.com/44221447/178153014-815662c1-21f9-4cc0-b637-9f8cf65f96b3.png" alt="멀티미디어" /></p>

<p>역시 유용하게 사용되는 키다.</p>

<pre><code class="language-ahk">;***************************************
;   캡스락 설정
;***************************************
*CapsLock:: fn:=1
*CapsLock Up:: fn:=0
LWin &amp; Space:: CapsLock ; 캡스락 대체키

;***************************************
;   멀티미디어 설정
;***************************************
#if fn
[::Volume_Down
]::Volume_Up
m::Volume_Mute
</code></pre>

<h3 id="기타">기타</h3>

<p><img src="https://user-images.githubusercontent.com/44221447/178153096-d46a374e-e67d-40bd-be2f-37fe0a22de84.png" alt="기타" /></p>

<p>해피해킹 키보드에서는 일반적인 CapsLock 자리에 Ctrl이 존재하고, 실제로 복사, 붙여넣기 단축키인 <code class="language-plaintext highlighter-rouge">Ctrl + c</code>, <code class="language-plaintext highlighter-rouge">Ctrl + v</code>를 할 때 <code class="language-plaintext highlighter-rouge">Ctrl</code> 위치가 <code class="language-plaintext highlighter-rouge">CapsLock</code> 자리에 있으면 훨씬 손목을 덜 꺾으면서 손도 벗어나지 않으면서 할 수 있다.</p>

<p>그래서 <code class="language-plaintext highlighter-rouge">CapsLock + c</code>, <code class="language-plaintext highlighter-rouge">CapsLock + v</code>로 복사, 붙여넣기 할 수 있도록 만들었다.</p>

<p>H 자리에는 <code class="language-plaintext highlighter-rouge">~</code>를 넣었는데, 앤프로2에서 <code class="language-plaintext highlighter-rouge">~</code>를 적으려면 굉장히 불편했기에 저 자리에 매핑했던 것이 손에 익어서 넣어봤다.</p>

<pre><code class="language-ahk">;***************************************
;   캡스락 설정
;***************************************
*CapsLock:: fn:=1
*CapsLock Up:: fn:=0
LWin &amp; Space:: CapsLock ; 캡스락 대체키

;***************************************
;   단축키 설정
;***************************************
#if fn
c::Send, {CtrlDown}{c} ; COPY
v::Send, {CtrlDown}{v} ; PASTE

;***************************************
;   기타 설정
;***************************************
h::Send, ~ ; WAVE
</code></pre>

<h2 id="완료된-모습">완료된 모습</h2>

<p><img src="https://user-images.githubusercontent.com/44221447/178153504-a99cdf6b-47f8-4cf6-b908-4abc444efd8f.png" alt="total" /></p>

<pre><code class="language-ahk">;***************************************
;   캡스락 설정
;***************************************
*CapsLock:: fn:=1
*CapsLock Up:: fn:=0
LWin &amp; Space:: CapsLock ; 캡스락 대체키

;***************************************
;   방향키 설정
;***************************************
; 기본 WASD, IJKL
#if fn
j::Send, {Left} ; LEFT
a::Send, {Left}
k::Send, {Down} ; DOWN
s::Send, {Down}
l::Send, {Right} ; RIGHT
d::Send, {Right}
i::Send, {Up} ; UP
w::Send, {Up}

; Shift 조합
+j::Send, {ShiftDown}{Left} ; LEFT
+a::Send, {ShiftDown}{Left}
+k::Send, {ShiftDown}{Down} ; DOWN
+s::Send, {ShiftDown}{Down}
+l::Send, {ShiftDown}{Right} ; RIGHT
+d::Send, {ShiftDown}{Right}
+w::Send, {ShiftDown}{Up}
+i::Send, {ShiftDown}{Up} ; UP

; Ctrl 조합
^j::Send, {CtrlDown}{Left} ; LEFT
^a::Send, {CtrlDown}{Left}
^k::Send, {CtrlDown}{Down} ; DOWN
^s::Send, {CtrlDown}{Down}
^l::Send, {CtrlDown}{Right} ; RIGHT
^d::Send, {CtrlDown}{Right}
^i::Send, {CtrlDown}{Up} ; UP
^w::Send, {CtrlDown}{Up}

; Alt 조합
!j::Send, {AltDown}{Left} ; LEFT
!a::Send, {AltDown}{Left}
!k::Send, {AltDown}{Down} ; DOWN
!s::Send, {AltDown}{Down}
!l::Send, {AltDown}{Right} ; RIGHT
!d::Send, {AltDown}{Right}
!i::Send, {AltDown}{Up} ; UP
!w::Send, {AltDown}{Up}

; Ctrl Shift 조합
^+j::Send, {CtrlDown}{ShiftDown}{Left} ; LEFT
^+a::Send, {CtrlDown}{ShiftDown}{Left}
^+k::Send, {CtrlDown}{ShiftDown}{Down} ; DOWN
^+s::Send, {CtrlDown}{ShiftDown}{Down}
^+l::Send, {CtrlDown}{ShiftDown}{Right} ; RIGHT
^+d::Send, {CtrlDown}{ShiftDown}{Right}
^+i::Send, {CtrlDown}{ShiftDown}{Up} ; UP
^+w::Send, {CtrlDown}{ShiftDown}{Up}

;***************************************
;   HOME, END, PGUP, PGDN 설정
;***************************************
`;::Send, {Home} ; HOME
'::Send, {End} ; END
q::Send, {PgUp} ; PageUp
e::Send, {PgDn} ; PageDown

; Shift 조합
+`;::Send, {ShiftDown}{Home} ; HOME
+'::Send, {ShiftDown}{End} ; END
+q::Send, {ShiftDown}{PgUp} ; PageUp
+e::Send, {ShiftDown}{PgDn} ; PageDown

; Alt 조합
!`;::Send, {AltDown}{Home} ; HOME
!'::Send, {AltDown}{End} ; END
!q::Send, {AltDown}{PgUp} ; PageUp
!e::Send, {AltDown}{PgDn} ; PageDown

;***************************************
;   Insert, Delete 설정
;***************************************
.::Send, {Insert} ; INSERT
/::Send, {Delete} ; DELETE

; Alt 조합
!.::Send, {AltDown}{Insert} ; INSERT
!/::Send, {AltDown}{Delete} ; DELETE

; Shift 조합
+.::Send, {ShiftDown}{Insert} ; INSERT
+/::Send, {ShiftDown}{Delete} ; DELETE

; Ctrl 조합
^.::Send, {CtrlDown}{Insert} ; INSERT
^/::Send, {CtrlDown}{Delete} ; DELETE

; Ctrl + Alt 조합
^!.::Send, {CtrlDown}{AltDown}{Insert} ; INSERT
^!/::Send, {CtrlDown}{AltDown}{Delete} ; DELETE

;***************************************
;   F키 설정
;***************************************
1::Send, {F1}
2::Send, {F2}
3::Send, {F3}
4::Send, {F4}
5::Send, {F5}
6::Send, {F6}
7::Send, {F7}
8::Send, {F8}
9::Send, {F9}
0::Send, {F10}
-::Send, {F11}
=::Send, {F12}

; Shift 조합
+1::Send, {ShiftDown}{F1}
+2::Send, {ShiftDown}{F2}
+3::Send, {ShiftDown}{F3}
+4::Send, {ShiftDown}{F4}
+5::Send, {ShiftDown}{F5}
+6::Send, {ShiftDown}{F6}
+7::Send, {ShiftDown}{F7}
+8::Send, {ShiftDown}{F8}
+9::Send, {ShiftDown}{F9}
+0::Send, {ShiftDown}{F10}
+-::Send, {ShiftDown}{F11}
+=::Send, {ShiftDown}{F12}

; Ctrl 조합
^1::Send, {CtrlDown}{F1}
^2::Send, {CtrlDown}{F2}
^3::Send, {CtrlDown}{F3}
^4::Send, {CtrlDown}{F4}
^5::Send, {CtrlDown}{F5}
^6::Send, {CtrlDown}{F6}
^7::Send, {CtrlDown}{F7}
^8::Send, {CtrlDown}{F8}
^9::Send, {CtrlDown}{F9}
^0::Send, {CtrlDown}{F10}
^-::Send, {CtrlDown}{F11}
^=::Send, {CtrlDown}{F12}

; Ctrl + Shift 조합
^+1::Send, {CtrlDown}{ShiftDown}{F1}
^+2::Send, {CtrlDown}{ShiftDown}{F2}
^+3::Send, {CtrlDown}{ShiftDown}{F3}
^+4::Send, {CtrlDown}{ShiftDown}{F4}
^+5::Send, {CtrlDown}{ShiftDown}{F5}
^+6::Send, {CtrlDown}{ShiftDown}{F6}
^+7::Send, {CtrlDown}{ShiftDown}{F7}
^+8::Send, {CtrlDown}{ShiftDown}{F8}
^+9::Send, {CtrlDown}{ShiftDown}{F9}
^+0::Send, {CtrlDown}{ShiftDown}{F10}
^+-::Send, {CtrlDown}{ShiftDown}{F11}
^+=::Send, {CtrlDown}{ShiftDown}{F12}

; Alt 조합
!1::Send, {AltDown}{F1}
!2::Send, {AltDown}{F2}
!3::Send, {AltDown}{F3}
!4::Send, {AltDown}{F4}
!5::Send, {AltDown}{F5}
!6::Send, {AltDown}{F6}
!7::Send, {AltDown}{F7}
!8::Send, {AltDown}{F8}
!9::Send, {AltDown}{F9}
!0::Send, {AltDown}{F10}
!-::Send, {AltDown}{F11}
!=::Send, {AltDown}{F12}

;***************************************
;   멀티미디어 설정
;***************************************
[::Volume_Down
]::Volume_Up
m::Volume_Mute

;***************************************
;   단축키 설정
;***************************************
c::Send, {CtrlDown}{c} ; COPY
v::Send, {CtrlDown}{v} ; PASTE

;***************************************
;   기타 설정
;***************************************
h::Send, ~ ; WAVE
</code></pre>

<h2 id="시작-프로그램에-등록">시작 프로그램에 등록</h2>

<hr />

<p>스크립트를 만들었다면 <code class="language-plaintext highlighter-rouge">F5</code>를 눌러서 실행할 수 있다. 하지만 컴퓨터를 재부팅하면 다시 파일을 실행시켜야 하는 불편함이 있다.</p>

<p>이건 [시작 프로그램] 폴더에 스크립트를 넣어놓으면 간단히 해결된다.</p>

<h3 id="바로가기-파일-생성">바로가기 파일 생성</h3>

<p><img src="https://user-images.githubusercontent.com/44221447/178153645-b398185d-6fca-4be2-bdb6-2dc02b9bb4c5.png" alt="바로가기" /></p>

<p>마우스 우클릭으로 바로가기 파일을 만들거나 <code class="language-plaintext highlighter-rouge">Alt</code>를 누른 채로 파일을 드래그하면 바로가기 파일이 만들어진다.</p>

<h3 id="시작-프로그램에-넣기">시작 프로그램에 넣기</h3>

<p><img src="https://user-images.githubusercontent.com/44221447/178153746-c1cda12f-1c45-43db-833a-1d2809f9cadb.png" alt="폴더" /></p>

<p><code class="language-plaintext highlighter-rouge">Win + R</code>을 눌러서 실행을 켜고 <code class="language-plaintext highlighter-rouge">Shell:startup</code> 을 타이핑하고 확인 버튼을 누르면 시작 프로그램 폴더가 열린다. 이 곳에 위에서 만든 바로가기 파일을 넣는다.</p>]]></content><author><name>mangbaam</name></author><category term="TIP" /><category term="autohotkey" /><category term="keymapping" /><category term="tip" /><summary type="html"><![CDATA[⭐]]></summary></entry><entry><title type="html">Coroutine suspend function</title><link href="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/07/coroutine-basic-dive-2.html" rel="alternate" type="text/html" title="Coroutine suspend function" /><published>2022-07-07T00:00:00+00:00</published><updated>2022-07-07T00:00:00+00:00</updated><id>https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/07/coroutine-basic-dive-2</id><content type="html" xml:base="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/07/coroutine-basic-dive-2.html"><![CDATA[<h2>⭐</h2>

<blockquote>
  <p>본 글은 <a href="https://myungpyo.medium.com/%EC%BD%94%EB%A3%A8%ED%8B%B4-%EA%B3%B5%EC%8B%9D-%EA%B0%80%EC%9D%B4%EB%93%9C-%EC%9E%90%EC%84%B8%ED%9E%88-%EC%9D%BD%EA%B8%B0-part-1-dive-3-b174c735d4fa">suspend 함수란 무엇이고 어떻게 동작할까?</a>을 읽고 공부한 내용을 정리하는 글입니다.</p>
</blockquote>

<p>코루틴을 사용해봤다면 <code class="language-plaintext highlighter-rouge">suspend</code> 키워드가 낯설지 않을 것이다. 중단(suspend)함수 없는 코루틴은 상상할 수 없다.</p>

<p>일반 함수(순차 함수)가 호출된 후 함수의 모든 내용을 모두 수행한 후 반환되는 것에 반해 중단 함수는 함수 내용을 수행 중 잠시 중단되어 다른 로직을 수행한 뒤 다시 돌아올 수 있다는 특징이 있다.</p>

<p>단순히 <code class="language-plaintext highlighter-rouge">suspend</code> 키워드를 붙이는 것으로 어떻게 중단 함수가 만들어지고, 동작하는지 알아보고자 한다.</p>

<h2 id="cps-continuation-passing-style">CPS: Continuation-Passing-Style</h2>

<hr />

<p><code class="language-plaintext highlighter-rouge">suspend</code> 키워드가 붙은 함수는 코틀린 컴파일러에 의해 <strong>CPS(Continuation-Passing-Style)</strong> 로 동작하게 된다.</p>

<blockquote>
  <p>CPS: <code class="language-plaintext highlighter-rouge">Continutation</code> 파라미터가 함수의 마지막 인자로 추가되며 반환 값이 <code class="language-plaintext highlighter-rouge">Any?</code> 로 변경됨</p>
</blockquote>

<p>CPS로 변환된 중단 함수는 코루틴 스코프나 다른 중단 함수 내에서만 호출될 수 있지만 코루틴 실행 흐름을 일시 중단하는 분절점이 될 수 있다.</p>

<p><code class="language-plaintext highlighter-rouge">suspend</code> 키워드를 붙여서 중단 함수로 만들 수 있는 함수는 <strong>탑 레벨 함수</strong>, <strong>확장 함수</strong>, <strong>멤버 함수(메서드)</strong>, <strong>로컬 함수</strong>, <strong>연산자(operator) 함수</strong> 등 <strong><em>모든 함수</em></strong>가 가능하다.</p>

<h3 id="continuation">Continuation</h3>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@SinceKotlin</span><span class="p">(</span><span class="s">"1.3"</span><span class="p">)</span>
<span class="k">public</span> <span class="kd">interface</span> <span class="nc">Continuation</span><span class="p">&lt;</span><span class="k">in</span> <span class="nc">T</span><span class="p">&gt;</span> <span class="p">{</span>
    <span class="cm">/**
     * The context of the coroutine that corresponds to this continuation.
     */</span>
    <span class="k">public</span> <span class="kd">val</span> <span class="py">context</span><span class="p">:</span> <span class="nc">CoroutineContext</span>

    <span class="cm">/**
     * Resumes the execution of the corresponding coroutine passing a successful or failed [result] as the
     * return value of the last suspension point.
     */</span>
    <span class="k">public</span> <span class="k">fun</span> <span class="nf">resumeWith</span><span class="p">(</span><span class="n">result</span><span class="p">:</span> <span class="nc">Result</span><span class="p">&lt;</span><span class="nc">T</span><span class="p">&gt;)</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="중단-과정">중단 과정</h2>

<hr />

<p>중단 함수가 코루틴에서 호출되면 그 시점의 실행 정보들을 <code class="language-plaintext highlighter-rouge">Continutation</code> 객체로 만들어서 캐시해 두었다가 실행이 재개되면 저장된 실행 정보를 기반으로 실행을 이어 나간다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">suspend</span> <span class="k">fun</span> <span class="nf">sum</span><span class="p">(</span><span class="n">num1</span><span class="p">:</span> <span class="nc">Int</span><span class="p">,</span> <span class="n">num2</span><span class="p">:</span> <span class="nc">Int</span><span class="p">):</span> <span class="nc">Int</span> <span class="p">{</span>
    <span class="nf">delay</span><span class="p">(</span><span class="mi">2000</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">num1</span> <span class="p">+</span> <span class="n">num2</span>
<span class="p">}</span>
</code></pre></div></div>

<p>위 코드처럼 2초가 걸리는 작업이 있다고 해보자. 이 함수는 중단 함수이기 때문에 코루틴이나 다른 중단 함수 내부에서 호출되어야 한다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">job</span> <span class="p">=</span> <span class="nc">GlobalScope</span><span class="p">.</span><span class="nf">launch</span> <span class="p">{</span>
        <span class="kd">val</span> <span class="py">result</span> <span class="p">=</span> <span class="nf">sum</span><span class="p">(</span><span class="mi">100</span><span class="p">,</span> <span class="mi">200</span><span class="p">)</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"Result : $result"</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="n">job</span><span class="p">.</span><span class="nf">join</span><span class="p">()</span>
<span class="p">}</span>
</code></pre></div></div>

<p>GlobalScope.launch으로 생성된 코루틴 내에서 <code class="language-plaintext highlighter-rouge">sum()</code> 중단 함수가 호출되었다.</p>

<p>작성된 코드를 바이트 코드로 바꿔서 보면 다음과 같은 형식으로 만들어지는 것을 볼 수 있다. (<a href="#바이트-코드-확인하는-법">바이트 코드 보는 법</a>)</p>

<p><code class="language-plaintext highlighter-rouge">INVOKESTATIC coroutine/SuspendKt.sum (IILkotlin/coroutines/Continuation;)Ljava/lang/Object;</code></p>

<p><img src="https://user-images.githubusercontent.com/44221447/177835615-6e382893-8b27-462a-951c-524936cb2bc0.png" alt="byte" /></p>

<p><code class="language-plaintext highlighter-rouge">INVOKESTATIC</code>은 정적 메서드를 실행하는 <a href="https://docs.oracle.com/javase/specs/jvms/se7/html/jvms-6.html">자바 가상 머신 명령</a>이다. sum 함수가 자바 코드로 변환되면 정적 메서드로 변환되기 때문이다.</p>

<p><code class="language-plaintext highlighter-rouge">coroutine/SuspendKt.sum</code> 은 coroutine 패키지의 suspend.kt 파일에 있는 sum 함수를 뜻한다. 자바 코드로 변환하면 suspend.kt 파일은 SuspendKt 클래스로 변환되고, sum 함수는 SuspendKt 클래스의 정적 메서드로 변환된다.</p>

<p><code class="language-plaintext highlighter-rouge">IILkotlin/coroutines/Continuation;</code> 부분은 sum 함수 파라미터 형식을 나타낸다. <strong>I: Integer, I: Integer, L: Class Instance</strong> 즉, <code class="language-plaintext highlighter-rouge">sum(int, int, kotlin.coroutines.Continuation)</code> 으로 변환될 것을 의미한다.</p>

<p>그리고 컴파일러가 함수의 마지막 파라미터에 <code class="language-plaintext highlighter-rouge">Continuation</code> object를 추가한 것을 볼 수 있다.</p>

<p><img src="https://user-images.githubusercontent.com/44221447/177836872-6f3a6132-f0de-47b9-9cdc-d96960bf7aa9.png" alt="디컴파일" /></p>

<p>바이트 코드를 디컴파일 해보면 위 사진과 같이 <code class="language-plaintext highlighter-rouge">SuspendKt</code>라는 클래스가 만들어지고 sum 함수가 정적(static) 메서드로 변환된 것을 확인할 수 있다. 또한 마지막 파라미터에 <code class="language-plaintext highlighter-rouge">Continuation</code> 객체가 추가된 것도 보인다.</p>

<p><code class="language-plaintext highlighter-rouge">Continutaion</code>이 추가되면서 <strong>CPS</strong> 방식으로 함수가 사용될 수 있게 되었고, 코루틴 프레임워크는 이를 통해 중단/재개 전환 동작을 수행할 수 있게 된다.</p>

<h3 id="중단-예제">중단 예제</h3>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="p">(</span><span class="mi">1</span><span class="o">..</span><span class="mi">2</span><span class="p">).</span><span class="nf">forEach</span> <span class="p">{</span> <span class="n">num</span> <span class="p">-&gt;</span>
        <span class="nf">launch</span> <span class="p">{</span>
            <span class="nf">longRunningTask</span><span class="p">(</span><span class="n">num</span><span class="p">,</span> <span class="n">num</span> <span class="p">+</span> <span class="mi">1</span><span class="p">)</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="k">suspend</span> <span class="k">fun</span> <span class="nf">longRunningTask</span><span class="p">(</span><span class="n">p1</span><span class="p">:</span> <span class="nc">Int</span><span class="p">,</span> <span class="n">p2</span><span class="p">:</span> <span class="nc">Int</span><span class="p">):</span> <span class="nc">Int</span> <span class="p">{</span>
    <span class="nf">log</span><span class="p">(</span><span class="s">"계산 시작: p1: $p1, p2: $p2"</span><span class="p">)</span>
    <span class="nf">delay</span><span class="p">(</span><span class="mi">2000</span><span class="p">)</span>
    <span class="kd">val</span> <span class="py">intermediateResult</span> <span class="p">=</span> <span class="n">p1</span> <span class="p">+</span> <span class="n">p2</span>
    <span class="nf">log</span><span class="p">(</span><span class="s">"계산 중간 결과 (p1 + p2) = $intermediateResult"</span><span class="p">)</span>
    <span class="nf">delay</span><span class="p">(</span><span class="mi">2000</span><span class="p">)</span>
    <span class="kd">val</span> <span class="py">finalResult</span> <span class="p">=</span> <span class="n">intermediateResult</span> <span class="p">*</span> <span class="mi">2</span>
    <span class="nf">log</span><span class="p">(</span><span class="s">"계산 종료 (result x 2) : $finalResult"</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">finalResult</span>
<span class="p">}</span>

<span class="k">private</span> <span class="k">fun</span> <span class="nf">log</span><span class="p">(</span><span class="n">message</span><span class="p">:</span> <span class="nc">String</span><span class="p">)</span> <span class="p">{</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"[${Thread.currentThread().name}] : $message"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>위 예제에서는 2개의 코루틴을 만들어 2번의 delay() 함수를 호출하는 <code class="language-plaintext highlighter-rouge">longRunningTask</code>가 main 함수에서 2번 호출된다.</p>

<p>만약 longRunningTask 함수가 일반 함수였다면 longRunningTask(1, 2)가 4초간 모든 계산 결과를 마친 후에 longRunningTask(2, 3)이 수행되어 8초 이상이 소요됐을 것이다.</p>

<p>하지만 중단 함수로 만들어진 예제를 실행시켜 보면 다음과 같은 결과를 얻을 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[main] : 계산 시작: p1: 1, p2: 2
[main] : 계산 시작: p1: 2, p2: 3
[main] : 계산 중간 결과 (p1 + p2) = 3
[main] : 계산 중간 결과 (p1 + p2) = 5
[main] : 계산 종료 (result x 2) : 6
[main] : 계산 종료 (result x 2) : 10
</code></pre></div></div>

<p>실행 결과를 보아 첫 번째 중단 함수가 delay()를 만난 동안 두 번째 중단 함수가 수행되어 번갈아가면 수행되는 것을 알 수 있다. delay() 를 만난 시점에서 잠시 중단되었다고 할 수 있는데, 이 지점을 중단점(suspension point)라고 하자.</p>

<p>중단 함수가 중단점을 만나면 다른 함수에게 실행 기회가 주어진다. 이런 식으로 하나의 스레드 안에서 실행 시간을 분할해가며 수행할 수 있다.</p>

<h2 id="중첩된-중단-함수">중첩된 중단 함수</h2>

<hr />

<p>그렇다면 중단 함수 안에서 또 다른 중단 함수를 호출하면 어떻게 될까?</p>

<p><img src="https://user-images.githubusercontent.com/44221447/177974256-24e79159-da3e-4472-ab2a-c78b631097ee.png" alt="stack" /></p>

<p>위 그림을 보면 일반적으로 순차 함수 안에서 다른 함수를 호출할 때 스택에 쌓이는 모습과 미슷하게 동작하게 된다.</p>

<p>다만 차이점이 있다면 일반 함수 호출은 OS에서 콜 스택을 관리해주지만 중첩 코루틴이나 중첩 중단 함수에서는 코루틴 프레임워크가 CPS 방식으로 호출 정보(Continuation)를 스택 형태로 유지하다가 호출 스택의 가장 마지막 함수가 실행을 종료할 때 결과 값이 직전 호출 함수들로 전파되며 직전 함수를 재개하게 된다.</p>

<p>만약 스택 상에서 어떤 함수가 예외를 발생시키면 이 예외는 최초 호출 함수까지 Continuation을 통해 전파된다.</p>

<h2 id="부록">부록</h2>

<hr />

<h3 id="바이트-코드-확인하는-법">바이트 코드 확인하는 법</h3>

<p>젯브레인 IDE(인텔리제이, 안드로이드 스튜디오 등)에서 할 수 있는 방법이다.</p>

<p><img src="https://user-images.githubusercontent.com/44221447/177835433-c5dc8ac0-1951-4b82-a571-bb93e2856779.png" alt="bytecode" /></p>

<blockquote>
  <p>도구 - Kotlin - Kotlin 바이트코드 표시</p>
</blockquote>]]></content><author><name>mangbaam</name></author><category term="Kotlin" /><category term="kotlin" /><category term="android" /><category term="coroutine" /><summary type="html"><![CDATA[⭐]]></summary></entry><entry><title type="html">CoroutineContext and CoroutineScope in Kotlin and Android</title><link href="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/06/coroutine-basic-dive-1.html" rel="alternate" type="text/html" title="CoroutineContext and CoroutineScope in Kotlin and Android" /><published>2022-07-06T00:00:00+00:00</published><updated>2022-07-06T00:00:00+00:00</updated><id>https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/06/coroutine-basic-dive-1</id><content type="html" xml:base="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/06/coroutine-basic-dive-1.html"><![CDATA[<h2>⭐</h2>

<blockquote>
  <p>본 글은 <a href="https://myungpyo.medium.com/reading-coroutine-official-guide-thoroughly-part-1-7ebb70a51910">CoroutineContext 와 CoroutineScope 란 무엇인가?</a>을 읽고 공부한 내용을 정리하는 글입니다.</p>
</blockquote>

<h2 id="coroutinecontextkt">CoroutineContext.kt</h2>

<hr />

<p>다음 코드들은 <strong>CoroutineContext.kt</strong> 파일에 있는 코드들이다.</p>

<h3 id="coroutinecontext-인터페이스">CoroutineContext 인터페이스</h3>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">public</span> <span class="kd">interface</span> <span class="nc">CoroutineContext</span> <span class="p">{</span>
  <span class="cm">/**
   * Returns the element with the given [key] from this context or `null`.
   * Keys are compared _by reference_, that is to get an element from the context the reference to its actual key
   * object must be presented to this function.
   */</span>
  <span class="k">public</span> <span class="k">operator</span> <span class="k">fun</span> <span class="p">&lt;</span><span class="nc">E</span> <span class="p">:</span> <span class="nc">Element</span><span class="p">&gt;</span> <span class="nf">get</span><span class="p">(</span><span class="n">key</span><span class="p">:</span> <span class="nc">Key</span><span class="p">&lt;</span><span class="nc">E</span><span class="p">&gt;):</span> <span class="nc">E</span><span class="p">?</span>
  <span class="cm">/**
   * Accumulates entries of this context starting with [initial] value and applying [operation]
   * from left to right to current accumulator value and each element of this context.
   */</span>
  <span class="k">public</span> <span class="k">fun</span> <span class="p">&lt;</span><span class="nc">R</span><span class="p">&gt;</span> <span class="nf">fold</span><span class="p">(</span><span class="n">initial</span><span class="p">:</span> <span class="nc">R</span><span class="p">,</span> <span class="n">operation</span><span class="p">:</span> <span class="p">(</span><span class="nc">R</span><span class="p">,</span> <span class="nc">Element</span><span class="p">)</span> <span class="p">-&gt;</span> <span class="nc">R</span><span class="p">):</span> <span class="nc">R</span>
  <span class="cm">/**
   * Returns a context containing elements from this context and elements from  other [context].
   * The elements from this context with the same key as in the other one are dropped.
   */</span>
  <span class="k">public</span> <span class="k">operator</span> <span class="k">fun</span> <span class="nf">plus</span><span class="p">(</span><span class="n">context</span><span class="p">:</span> <span class="nc">CoroutineContext</span><span class="p">):</span> <span class="nc">CoroutineContext</span> <span class="p">=</span> <span class="o">..</span><span class="p">.</span><span class="n">impl</span><span class="o">..</span><span class="p">.</span>
  <span class="cm">/**
   * Returns a context containing elements from this context, but without an element with
   * the specified [key]. Keys are compared _by reference_, that is to remove an element from the context
   * the reference to its actual key object must be presented to this function.
   */</span>
  <span class="k">public</span> <span class="k">fun</span> <span class="nf">minusKey</span><span class="p">(</span><span class="n">key</span><span class="p">:</span> <span class="nc">Key</span><span class="p">&lt;</span><span class="err">*</span><span class="p">&gt;):</span> <span class="nc">CoroutineContext</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>CoroutineContext</strong> 인터페이스에는 4개의 메서드가 있다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">get()</code>: 주어진 key에 해당하는 컨텍스트 요소를 반환한다.</li>
  <li><code class="language-plaintext highlighter-rouge">fold()</code>: 초기값(<code class="language-plaintext highlighter-rouge">initial</code>)을 시작으로 제공된 병합 함수(<code class="language-plaintext highlighter-rouge">operation</code>)를 사용해 대상 컨텍스트 요소들을 병합한 결과를 반환한다. 예를 들어 초기값에 <code class="language-plaintext highlighter-rouge">EmptyCoroutineContext</code>를 주고 특정 컨텍스트 요소들만 찾아 추가하는 함수를 주면 해당 요소만으로 구성된 코루틴 컨텍스트를 만들 수 있다.</li>
  <li><code class="language-plaintext highlighter-rouge">plus()</code>: 현재 컨텍스트와 매개변수로 받은 <code class="language-plaintext highlighter-rouge">context</code>가 갖는 모든 요소들을 포함하는 컨텍스트를 반환한다. 현재 컨텍스트 요소와 <code class="language-plaintext highlighter-rouge">context</code>의 요소 중 중복되는 것은 현재 컨텍스트에서 중복 제거 한다.</li>
  <li><code class="language-plaintext highlighter-rouge">minusKey()</code>: 현재 컨텍스트에서 주어진 키를 갖는 요소들을 제외한 새로운 컨텍스트를 반환한다.</li>
</ul>

<h3 id="key-인터페이스">Key 인터페이스</h3>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/**
 * Key for the elements of [CoroutineContext]. [E] is a type of element with this key.
 * Keys in the context are compared _by reference_.
 */</span>
<span class="k">public</span> <span class="kd">interface</span> <span class="nc">Key</span><span class="p">&lt;</span><span class="nc">E</span> <span class="p">:</span> <span class="nc">Element</span><span class="p">&gt;</span>

</code></pre></div></div>

<p><strong>Key</strong>는 <code class="language-plaintext highlighter-rouge">Element</code> 타입을 제네릭 타입으로 가진다.</p>

<h3 id="element-인터페이스">Element 인터페이스</h3>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/**
 * An element of the [CoroutineContext]. An element of the coroutine context is a singleton context by itself.
 */</span>
<span class="k">public</span> <span class="kd">interface</span> <span class="nc">Element</span> <span class="p">:</span> <span class="nc">CoroutineContext</span> <span class="p">{</span>
  <span class="cm">/**
   * A key of this coroutine context element.
   */</span>
  <span class="k">public</span> <span class="kd">val</span> <span class="py">key</span><span class="p">:</span> <span class="nc">Key</span><span class="p">&lt;</span><span class="err">*</span><span class="p">&gt;</span>
  
  <span class="o">..</span><span class="p">.</span><span class="n">overrides</span><span class="o">..</span><span class="p">.</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">Element</code>는 <code class="language-plaintext highlighter-rouge">CoroutineContext</code>를 상속하고, key를 멤버 속성으로 갖는다.</p>

<p><code class="language-plaintext highlighter-rouge">CoroutineContext</code>를 구성하는 <code class="language-plaintext highlighter-rouge">Element</code>의 예로는 <code class="language-plaintext highlighter-rouge">CoroutineId</code>, <code class="language-plaintext highlighter-rouge">CoroutineName</code>, <code class="language-plaintext highlighter-rouge">CoroutineDispatcher</code>, <code class="language-plaintext highlighter-rouge">ContinuationInterceptor</code>, <code class="language-plaintext highlighter-rouge">CoroutineExceptionHandler</code> 등이 있다. 이런 요소(element)들은 각각의 key를 기반으로 <strong>CoroutineContext</strong>에 등록된다.</p>

<h3 id="요약">요약</h3>

<p>코루틴 컨텍스트(CoroutineContext)에는 코루틴 컨텍스트를 상속한 요소(Element) 들이 있고, 각 요소들이 등록될 때 요소의 고유한 키를 기반으로 등록된다는 것이다.</p>

<h3 id="coroutinecontext의-구현체">CoroutineContext의 구현체</h3>

<p><code class="language-plaintext highlighter-rouge">CoroutineContext</code> 인터페이스를 구현한 구현체는 다음 3가지가 있다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">EmptyCoroutineContext</code>: 컨텍스트가 명시되지 않은 경우 이 singleton 객체가 사용된다.</li>
  <li><code class="language-plaintext highlighter-rouge">CombinedContext</code>: 두 개 이상의 컨텍트스가 명시되면 컨텍스트 간 연결을 위한 컨테이너 역할을 하는 컨텍스트이다.</li>
  <li><code class="language-plaintext highlighter-rouge">Element</code>: 컨텍스트의 각 <a href="#element-인터페이스">요소</a>들도 <code class="language-plaintext highlighter-rouge">CoroutineContext</code>를 구현한다.</li>
</ul>

<h3 id="예시">예시</h3>

<p><img src="https://user-images.githubusercontent.com/44221447/177681983-a6ed6f56-b485-46ef-8ae6-acab19789347.png" alt="코루틴컨텍스트" /></p>

<p>위 그림은 <code class="language-plaintext highlighter-rouge">GlobalScope.launch {}</code>를 수행할 때 <code class="language-plaintext highlighter-rouge">launch</code> 함수의 첫 번째 인자인 <code class="language-plaintext highlighter-rouge">CoroutineContext</code>에 어떤 값을 넘기는 지에 따라 변화하는 코루틴 컨텍스트의 상태를 보여준다.</p>

<p>하나 하나 살펴보면 <code class="language-plaintext highlighter-rouge">CoroutineId</code>, <code class="language-plaintext highlighter-rouge">ContinuationIntercepter</code>, <code class="language-plaintext highlighter-rouge">CoroutineName</code>, <code class="language-plaintext highlighter-rouge">CoroutineExceptionHandler</code>가 보이는데 이것들 각각이 요소이다.</p>

<p>인자로 넘겨줄 때 <code class="language-plaintext highlighter-rouge">+</code> 연산자를 사용해 연결하고 있는데 이는 <a href="#coroutinecontext-인터페이스">CoroutineContext 인터페이스</a>가 <code class="language-plaintext highlighter-rouge">plus</code> 연산자(operator)를 구현하고 있기 때문이다.</p>

<p>이로 인해 각 요소가 병합되어 <code class="language-plaintext highlighter-rouge">CombinedContext</code>(병합된 CoroutineContext)가 만들어진다.</p>

<p>다시 그림을 보면 주황색 테두리로 묶여 있는 것을 확인할 수 있는데 이는 <code class="language-plaintext highlighter-rouge">CoroutineContext</code>와 <code class="language-plaintext highlighter-rouge">Element</code>가 묶여서 하나의 <code class="language-plaintext highlighter-rouge">CoroutineContext</code>(CombinedContext)가 된다는 것을 나타낸 것이다. 그리고 마지막에는 항상 <code class="language-plaintext highlighter-rouge">ContinuationInterceptor</code>가 존재하는데 이는 인터셉터로의 빠른 접근을 위해서라고 한다.</p>

<h4 id="코드-확인">코드 확인</h4>

<p><img src="https://user-images.githubusercontent.com/44221447/177527365-a142555d-6d2b-4f7a-807d-9bc104c76dfb.png" alt="런치" /></p>

<p><em><code class="language-plaintext highlighter-rouge">launch</code>의 첫 번째 인자로 코루틴 컨텍스트를 받는 것을 확인할 수 있다. 기본 값은 <code class="language-plaintext highlighter-rouge">EmptyCoroutineContext</code>이다.</em></p>

<h2 id="coroutinescope">CoroutineScope</h2>

<hr />

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">public</span> <span class="kd">interface</span> <span class="nc">CoroutineScope</span> <span class="p">{</span>
    <span class="cm">/**
     * Context of this scope.
     */</span>
    <span class="k">public</span> <span class="kd">val</span> <span class="py">coroutineContext</span><span class="p">:</span> <span class="nc">CoroutineContext</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">CoroutineScope</code>는 기본적으로 <code class="language-plaintext highlighter-rouge">CoroutineContext</code> 하나만 멤버 속성으로 정의하고 있는 인터페이스다.</p>

<p>우리가 사용하는 모든 코루틴 빌더(launch, async, coroutineScope, withContext 등)는 <code class="language-plaintext highlighter-rouge">CoroutineScope</code>의 확장 함수로 정의된다. 즉, 이 빌더들은 CoroutineScope의 함수들이고, 이것들로 코루틴을 생성할 때 소속된 CoroutineScope에 정의된 CoroutineContext를 기반으로 필요한 코루틴을 생성하게 된다.</p>

<h3 id="예제---안드로이드-activity">예제 - 안드로이드 Activity</h3>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">MyActivity</span> <span class="p">:</span> <span class="nc">AppCompatActivity</span><span class="p">(),</span> <span class="nc">CoroutineScope</span> <span class="p">{</span>
  <span class="k">lateinit</span> <span class="kd">var</span> <span class="py">job</span><span class="p">:</span> <span class="nc">Job</span>
  <span class="k">override</span> <span class="kd">val</span> <span class="py">coroutineContext</span><span class="p">:</span> <span class="nc">CoroutineContext</span>
  <span class="k">get</span><span class="p">()</span> <span class="p">=</span> <span class="nc">Dispatchers</span><span class="p">.</span><span class="nc">Main</span> <span class="p">+</span> <span class="n">job</span>

  <span class="k">override</span> <span class="k">fun</span> <span class="nf">onCreate</span><span class="p">(</span><span class="n">savedInstanceState</span><span class="p">:</span> <span class="nc">Bundle</span><span class="p">?)</span> <span class="p">{</span>
    <span class="k">super</span><span class="p">.</span><span class="nf">onCreate</span><span class="p">(</span><span class="n">savedInstanceState</span><span class="p">)</span>
    <span class="n">job</span> <span class="p">=</span> <span class="nc">Job</span><span class="p">()</span>
  <span class="p">}</span>
  
  <span class="k">override</span> <span class="k">fun</span> <span class="nf">onDestroy</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">super</span><span class="p">.</span><span class="nf">onDestroy</span><span class="p">()</span>
    <span class="n">job</span><span class="p">.</span><span class="nf">cancel</span><span class="p">()</span>
  <span class="p">}</span>

  <span class="k">fun</span> <span class="nf">loadDataFromUI</span><span class="p">()</span> <span class="p">=</span> <span class="nf">launch</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">ioData</span> <span class="p">=</span> <span class="nf">async</span><span class="p">(</span><span class="nc">Dispatchers</span><span class="p">.</span><span class="nc">IO</span><span class="p">)</span> <span class="p">{</span>
      <span class="c1">// blocking I/O operation</span>
    <span class="p">}</span>

    <span class="c1">// do something else concurrently with I/O</span>
    <span class="kd">val</span> <span class="py">data</span> <span class="p">=</span> <span class="n">ioData</span><span class="p">.</span><span class="nf">await</span><span class="p">()</span> <span class="c1">// wait for result of I/O</span>
    <span class="nf">draw</span><span class="p">(</span><span class="n">data</span><span class="p">)</span> <span class="c1">// can draw in the main thread</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>위 코드는 안드로이드의 Activity에서 CoroutineScope를 구현하는 코드이다. CoroutineScope의 멤버인 CoroutineContext를 구현해야하는데 여기서는 <code class="language-plaintext highlighter-rouge">Dispatchers.Main + job</code>으로 정의했다. 이렇게 하면 MyActivity에서 생성되는 코루틴은 메인 스레드(UI Thread)로 디스패치 되고, 액티비티에서 생성한 Job 객체를 parent로 하는 Job들이 생성되면서 액티비티 <strong>Job의 생명 주기에 존속되게 된다</strong>. 즉, <em>parent의 Job을 cancel 하면 모든 자식 Job들도 cancel 된다.</em></p>

<h3 id="globalscopelaunch-가-의미하는-것">GlobalScope.launch 가 의미하는 것</h3>

<p>CoroutineScope는 CoroutineContext를 가지는 인터페이스라는 사실을 알았고, 이를 통해서 범위를 지정할 수 있다는 것도 <a href="#예제---안드로이드-activity">예제</a>를 통해 알 수 있었다.</p>

<p>그렇다면 <code class="language-plaintext highlighter-rouge">GlobalScope.launch {}</code> 코루틴 빌더는 무엇일까?</p>

<p>위 예제처럼 사용자 정의 스코프를 만들 도 있지만 편의를 위해 코루틴 프레임워크에 미리 정의된 스코프들이 있는데 <code class="language-plaintext highlighter-rouge">GlobalScope</code>가 그 중 하나이다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// -- in CoroutineScope.kt</span>
<span class="kd">object</span> <span class="nc">GlobalScope</span> <span class="p">:</span> <span class="nc">CoroutineScope</span> <span class="p">{</span>
    <span class="k">override</span> <span class="kd">val</span> <span class="py">coroutineContext</span><span class="p">:</span> <span class="nc">CoroutineContext</span>
        <span class="k">get</span><span class="p">()</span> <span class="p">=</span> <span class="nc">EmptyCoroutineContext</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// -- in CoroutineContextImpl.kt</span>
<span class="nd">@SinceKotlin</span><span class="p">(</span><span class="s">"1.3"</span><span class="p">)</span>
<span class="k">public</span> <span class="kd">object</span> <span class="nc">EmptyCoroutineContext</span> <span class="p">:</span> <span class="nc">CoroutineContext</span><span class="p">,</span> <span class="nc">Serializable</span> <span class="p">{</span>
    <span class="k">private</span> <span class="k">const</span> <span class="kd">val</span> <span class="py">serialVersionUID</span><span class="p">:</span> <span class="nc">Long</span> <span class="p">=</span> <span class="mi">0</span>
    <span class="k">private</span> <span class="k">fun</span> <span class="nf">readResolve</span><span class="p">():</span> <span class="nc">Any</span> <span class="p">=</span> <span class="nc">EmptyCoroutineContext</span>

    <span class="k">public</span> <span class="k">override</span> <span class="k">fun</span> <span class="p">&lt;</span><span class="nc">E</span> <span class="p">:</span> <span class="nc">Element</span><span class="p">&gt;</span> <span class="nf">get</span><span class="p">(</span><span class="n">key</span><span class="p">:</span> <span class="nc">Key</span><span class="p">&lt;</span><span class="nc">E</span><span class="p">&gt;):</span> <span class="nc">E</span><span class="p">?</span> <span class="p">=</span> <span class="k">null</span>
    <span class="k">public</span> <span class="k">override</span> <span class="k">fun</span> <span class="p">&lt;</span><span class="nc">R</span><span class="p">&gt;</span> <span class="nf">fold</span><span class="p">(</span><span class="n">initial</span><span class="p">:</span> <span class="nc">R</span><span class="p">,</span> <span class="n">operation</span><span class="p">:</span> <span class="p">(</span><span class="nc">R</span><span class="p">,</span> <span class="nc">Element</span><span class="p">)</span> <span class="p">-&gt;</span> <span class="nc">R</span><span class="p">):</span> <span class="nc">R</span> <span class="p">=</span> <span class="n">initial</span>
    <span class="k">public</span> <span class="k">override</span> <span class="k">fun</span> <span class="nf">plus</span><span class="p">(</span><span class="n">context</span><span class="p">:</span> <span class="nc">CoroutineContext</span><span class="p">):</span> <span class="nc">CoroutineContext</span> <span class="p">=</span> <span class="n">context</span>
    <span class="k">public</span> <span class="k">override</span> <span class="k">fun</span> <span class="nf">minusKey</span><span class="p">(</span><span class="n">key</span><span class="p">:</span> <span class="nc">Key</span><span class="p">&lt;</span><span class="err">*</span><span class="p">&gt;):</span> <span class="nc">CoroutineContext</span> <span class="p">=</span> <span class="k">this</span>
    <span class="k">public</span> <span class="k">override</span> <span class="k">fun</span> <span class="nf">hashCode</span><span class="p">():</span> <span class="nc">Int</span> <span class="p">=</span> <span class="mi">0</span>
    <span class="k">public</span> <span class="k">override</span> <span class="k">fun</span> <span class="nf">toString</span><span class="p">():</span> <span class="nc">String</span> <span class="p">=</span> <span class="s">"EmptyCoroutineContext"</span>
<span class="p">}</span>
</code></pre></div></div>

<p>위 코드는 <code class="language-plaintext highlighter-rouge">GlobalScope</code>의 구현 부분과 GlobalScope 내부에서 사용하는 <code class="language-plaintext highlighter-rouge">EmptyCoroutineContext</code>의 구현 부분이다.</p>

<p><code class="language-plaintext highlighter-rouge">GlobalScope</code>는 <strong>Singleton object</strong>로써 <code class="language-plaintext highlighter-rouge">EmptyCoroutineContext</code>를 컨텍스트로 가진다.</p>

<p><code class="language-plaintext highlighter-rouge">EmptyCoroutineContext</code>는 <code class="language-plaintext highlighter-rouge">CoroutineContext</code>에 대한 기본 구현만 한 컨텍스트이고, 어떤 생명주기에 바인딩 된 Job이 정의되지 않았기 때문에 <strong>애플리케이션 프로세스와 동일한 생명주기를 가지게 된다.</strong></p>

<h2 id="안드로이드의-coroutinescope">안드로이드의 CoroutineScope</h2>

<hr />

<p>안드로이드는 주로 Activity, Fragment, ViewModel 등의 컴포넌트에 작성이 되고, 이들은 각자의 생명주기를 가진다. 그리고 <code class="language-plaintext highlighter-rouge">androidx.lifecycle</code>에는 각 생명주기에 맞는 사전 정의된 스코프들이 있다.</p>

<h3 id="lifecyclescope">lifecycleScope</h3>

<p>lifecycleScope는 LifecycleOwner의 생명주기를 따른다. LifecycleOwner는 Activity, Fragment 등 다양한 컴포넌트에서 구현하고 있는 인터페이스이다. Activity, Fragment 등이 destroy 되면 이 스코프도 cancel 된다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">public</span> <span class="kd">val</span> <span class="py">LifecycleOwner</span><span class="p">.</span><span class="n">lifecycleScope</span><span class="p">:</span> <span class="nc">LifecycleCoroutineScope</span>
    <span class="k">get</span><span class="p">()</span> <span class="p">=</span> <span class="n">lifecycle</span><span class="p">.</span><span class="n">coroutineScope</span>
</code></pre></div></div>

<p>위 코드는 lifecycleScope의 구현 부분이고, LifecycleOwner의 확장 함수로 구현된 것을 알 수 있다.</p>

<p><code class="language-plaintext highlighter-rouge">lifecycle</code>은 <code class="language-plaintext highlighter-rouge">LifecycleOwner</code>에서 생명주기를 불러오는 부분이다. (자바에선 <code class="language-plaintext highlighter-rouge">getLifecycle()</code>)</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">public</span> <span class="k">abstract</span> <span class="kd">class</span> <span class="nc">LifecycleCoroutineScope</span> <span class="k">internal</span> <span class="k">constructor</span><span class="p">()</span> <span class="p">:</span> <span class="nc">CoroutineScope</span> <span class="p">{</span>
    <span class="k">internal</span> <span class="k">abstract</span> <span class="kd">val</span> <span class="py">lifecycle</span><span class="p">:</span> <span class="nc">Lifecycle</span>

    <span class="k">public</span> <span class="k">fun</span> <span class="nf">launchWhenCreated</span><span class="p">(</span><span class="n">block</span><span class="p">:</span> <span class="k">suspend</span> <span class="nc">CoroutineScope</span><span class="p">.()</span> <span class="p">-&gt;</span> <span class="nc">Unit</span><span class="p">):</span> <span class="nc">Job</span> <span class="p">=</span> <span class="nf">launch</span> <span class="p">{</span>
        <span class="n">lifecycle</span><span class="p">.</span><span class="nf">whenCreated</span><span class="p">(</span><span class="n">block</span><span class="p">)</span>
    <span class="p">}</span>

    <span class="k">public</span> <span class="k">fun</span> <span class="nf">launchWhenStarted</span><span class="p">(</span><span class="n">block</span><span class="p">:</span> <span class="k">suspend</span> <span class="nc">CoroutineScope</span><span class="p">.()</span> <span class="p">-&gt;</span> <span class="nc">Unit</span><span class="p">):</span> <span class="nc">Job</span> <span class="p">=</span> <span class="nf">launch</span> <span class="p">{</span>
        <span class="n">lifecycle</span><span class="p">.</span><span class="nf">whenStarted</span><span class="p">(</span><span class="n">block</span><span class="p">)</span>
    <span class="p">}</span>

    <span class="k">public</span> <span class="k">fun</span> <span class="nf">launchWhenResumed</span><span class="p">(</span><span class="n">block</span><span class="p">:</span> <span class="k">suspend</span> <span class="nc">CoroutineScope</span><span class="p">.()</span> <span class="p">-&gt;</span> <span class="nc">Unit</span><span class="p">):</span> <span class="nc">Job</span> <span class="p">=</span> <span class="nf">launch</span> <span class="p">{</span>
        <span class="n">lifecycle</span><span class="p">.</span><span class="nf">whenResumed</span><span class="p">(</span><span class="n">block</span><span class="p">)</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>위 코드는 <code class="language-plaintext highlighter-rouge">LifecycleCoroutineScope</code>의 코드이다. 추상 클래스이고, 반환 값을 보면 <code class="language-plaintext highlighter-rouge">CoroutineScope</code>로 되어있는 것을 알 수 있다.</p>

<p>그리고 <code class="language-plaintext highlighter-rouge">launchWhenCreated</code>, <code class="language-plaintext highlighter-rouge">launchWhenStated</code>, <code class="language-plaintext highlighter-rouge">launchWhenResumed</code>가 보인다. 각각은 이름 그대로 생명주기의 create, start, resume 상태에서 block을 수행하는 코루틴을 생성한다.</p>

<p><img src="https://user-images.githubusercontent.com/44221447/177607525-84163ad9-ed45-4b19-8ebc-928be20113b0.png" alt="caution" /></p>

<blockquote>
  <p>위 3개의 메서드에는 사진과 같은 경고성 주석이 달려있다. 특정 상황에 리소스를 낭비할 수 있으니 사용을 추천하지 않으며 미래에는 이 API들이 제거될 것이라고 한다. 대신 <code class="language-plaintext highlighter-rouge">Lifecycle.repeatOnLifecycle</code>을 사용하라고 한다.</p>
</blockquote>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nc">LifecycleOwner</span><span class="p">.</span><span class="nf">repeatOnStarted</span><span class="p">(</span><span class="n">block</span><span class="p">:</span> <span class="k">suspend</span> <span class="nc">CoroutineScope</span><span class="p">.()</span> <span class="p">-&gt;</span> <span class="nc">Unit</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">lifecycleScope</span><span class="p">.</span><span class="nf">launch</span> <span class="p">{</span>
        <span class="n">lifecycle</span><span class="p">.</span><span class="nf">repeatOnLifecycle</span><span class="p">(</span><span class="nc">Lifecycle</span><span class="p">.</span><span class="nc">State</span><span class="p">.</span><span class="nc">STARTED</span><span class="p">,</span> <span class="n">block</span><span class="p">)</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>좀 더 편하게 사용하기 위해 위와 같이 확장 함수로 만들어서 사용하기도 한다.</p>

<h3 id="viewmodelscope">viewModelScope</h3>

<p>viewModelScope는 AAC ViewModel의 생명주기를 따른다. 즉, ViewModel의 <code class="language-plaintext highlighter-rouge">onCleared</code>가 호출되는 시점에 이 스코프도 cancel 된다는 의미이다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">private</span> <span class="k">const</span> <span class="kd">val</span> <span class="py">JOB_KEY</span> <span class="p">=</span> <span class="s">"androidx.lifecycle.ViewModelCoroutineScope.JOB_KEY"</span>

<span class="cm">/**
 * This scope is bound to Dispatchers.Main.immediate
 */</span>
<span class="k">public</span> <span class="kd">val</span> <span class="py">ViewModel</span><span class="p">.</span><span class="n">viewModelScope</span><span class="p">:</span> <span class="nc">CoroutineScope</span>
    <span class="k">get</span><span class="p">()</span> <span class="p">{</span>
        <span class="kd">val</span> <span class="py">scope</span><span class="p">:</span> <span class="nc">CoroutineScope</span><span class="p">?</span> <span class="p">=</span> <span class="k">this</span><span class="p">.</span><span class="nf">getTag</span><span class="p">(</span><span class="nc">JOB_KEY</span><span class="p">)</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">scope</span> <span class="p">!=</span> <span class="k">null</span><span class="p">)</span> <span class="p">{</span>
            <span class="k">return</span> <span class="n">scope</span>
        <span class="p">}</span>
        <span class="k">return</span> <span class="nf">setTagIfAbsent</span><span class="p">(</span>
            <span class="nc">JOB_KEY</span><span class="p">,</span>
            <span class="nc">CloseableCoroutineScope</span><span class="p">(</span><span class="nc">SupervisorJob</span><span class="p">()</span> <span class="p">+</span> <span class="nc">Dispatchers</span><span class="p">.</span><span class="nc">Main</span><span class="p">.</span><span class="n">immediate</span><span class="p">)</span>
        <span class="p">)</span>
    <span class="p">}</span>

<span class="k">internal</span> <span class="kd">class</span> <span class="nc">CloseableCoroutineScope</span><span class="p">(</span><span class="n">context</span><span class="p">:</span> <span class="nc">CoroutineContext</span><span class="p">)</span> <span class="p">:</span> <span class="nc">Closeable</span><span class="p">,</span> <span class="nc">CoroutineScope</span> <span class="p">{</span>
    <span class="k">override</span> <span class="kd">val</span> <span class="py">coroutineContext</span><span class="p">:</span> <span class="nc">CoroutineContext</span> <span class="p">=</span> <span class="n">context</span>

    <span class="k">override</span> <span class="k">fun</span> <span class="nf">close</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">coroutineContext</span><span class="p">.</span><span class="nf">cancel</span><span class="p">()</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>위 코드는 <code class="language-plaintext highlighter-rouge">ViewModelScope</code>의 구현 부분이다. ViewModel의 확장 함수로 되어 있는 것을 알 수 있다. 그렇기 때문에 ViewModel의 <code class="language-plaintext highlighter-rouge">getTag</code>와 <code class="language-plaintext highlighter-rouge">setTagIfAbsent</code> 메소드를 사용할 수 있다.</p>

<p>로직을 살펴보면 <code class="language-plaintext highlighter-rouge">getTag</code>를 통해 ViewModel에 이미 <code class="language-plaintext highlighter-rouge">JOB_KEY</code>라는 키로 CoroutineScope가 저장되어 있었다면 해당 스코프를 반환하고, 저장되어 있지 않았다면 저장 후 (저장 한)스코프를 반환한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">abstract</span> <span class="kd">class</span> <span class="nc">ViewModel</span> <span class="o">{</span>
    <span class="nd">@Nullable</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">Object</span><span class="o">&gt;</span> <span class="n">mBagOfTags</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">HashMap</span><span class="o">&lt;&gt;();</span>
    <span class="nd">@Nullable</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">Set</span><span class="o">&lt;</span><span class="nc">Closeable</span><span class="o">&gt;</span> <span class="n">mCloseables</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">LinkedHashSet</span><span class="o">&lt;&gt;();</span>
    <span class="kd">private</span> <span class="kd">volatile</span> <span class="kt">boolean</span> <span class="n">mCleared</span> <span class="o">=</span> <span class="kc">false</span><span class="o">;</span>

    <span class="c1">// ...</span>

    <span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;</span> <span class="no">T</span> <span class="nf">setTagIfAbsent</span><span class="o">(</span><span class="nc">String</span> <span class="n">key</span><span class="o">,</span> <span class="no">T</span> <span class="n">newValue</span><span class="o">)</span> <span class="o">{</span>
        <span class="no">T</span> <span class="n">previous</span><span class="o">;</span>
        <span class="kd">synchronized</span> <span class="o">(</span><span class="n">mBagOfTags</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">previous</span> <span class="o">=</span> <span class="o">(</span><span class="no">T</span><span class="o">)</span> <span class="n">mBagOfTags</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="n">key</span><span class="o">);</span>
            <span class="k">if</span> <span class="o">(</span><span class="n">previous</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
                <span class="n">mBagOfTags</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="n">key</span><span class="o">,</span> <span class="n">newValue</span><span class="o">);</span>
            <span class="o">}</span>
        <span class="o">}</span>
        <span class="no">T</span> <span class="n">result</span> <span class="o">=</span> <span class="n">previous</span> <span class="o">==</span> <span class="kc">null</span> <span class="o">?</span> <span class="n">newValue</span> <span class="o">:</span> <span class="n">previous</span><span class="o">;</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">mCleared</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">closeWithRuntimeException</span><span class="o">(</span><span class="n">result</span><span class="o">);</span>
        <span class="o">}</span>
        <span class="k">return</span> <span class="n">result</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;</span> <span class="no">T</span> <span class="nf">getTag</span><span class="o">(</span><span class="nc">String</span> <span class="n">key</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">mBagOfTags</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">return</span> <span class="kc">null</span><span class="o">;</span>
        <span class="o">}</span>
        <span class="kd">synchronized</span> <span class="o">(</span><span class="n">mBagOfTags</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">return</span> <span class="o">(</span><span class="no">T</span><span class="o">)</span> <span class="n">mBagOfTags</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="n">key</span><span class="o">);</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>좀 더 자세히 살펴보자.</p>

<p>위 코드는 <code class="language-plaintext highlighter-rouge">ViewModel</code>의 구현 부분의 일부이다. ViewModel은 추상 클래스로 되어 있으며, <code class="language-plaintext highlighter-rouge">mBagOfTags</code>와 <code class="language-plaintext highlighter-rouge">mCloseables</code>, <code class="language-plaintext highlighter-rouge">mCleared</code> 멤버 속성을 가지고 있다.</p>

<p>ViewModelScope에서 <code class="language-plaintext highlighter-rouge">setTagIfAbsent</code>에 <code class="language-plaintext highlighter-rouge">JOB_KEY</code>라는 상수 값과 <code class="language-plaintext highlighter-rouge">SupervisorJob() + Dispatchers.Main.immediate</code>를 매개변수로 넘겨주는 부분이 있었다.</p>

<p><code class="language-plaintext highlighter-rouge">getTag</code>는 <code class="language-plaintext highlighter-rouge">mBagOfTags</code>에 key로 저장된 값이 없다면 null을, 있다면 해당 값을 반환한다. <code class="language-plaintext highlighter-rouge">setTagIfAbsent</code>는 key로 저장된 값이 없다면 newValue를 저장하고, 있다면 newValue는 무시된다. 그리고 저장된 값이 있었다면 저장되어 있던 값을, 없었다면 newValue를 result에 담아 반환하는데 그 전에 뷰모델이 clear 된 경우 result를 정리 후 반환하게 된다.</p>]]></content><author><name>mangbaam</name></author><category term="Kotlin" /><category term="kotlin" /><category term="android" /><category term="coroutine" /><summary type="html"><![CDATA[⭐]]></summary></entry><entry><title type="html">Kotlin Coroutine Basic</title><link href="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/03/coroutine-basic.html" rel="alternate" type="text/html" title="Kotlin Coroutine Basic" /><published>2022-07-03T00:00:00+00:00</published><updated>2022-07-03T00:00:00+00:00</updated><id>https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/03/coroutine-basic</id><content type="html" xml:base="https://mangbaam.github.io//jekyll-theme-yat/kotlin/2022/07/03/coroutine-basic.html"><![CDATA[<h2>⭐</h2>

<blockquote>
  <p>본 글은 <a href="https://myungpyo.medium.com/reading-coroutine-official-guide-thoroughly-part-1-98f6e792bd5b">코루틴 공식 가이드 읽고 분석하기 - Part1</a>을 읽고 공부한 내용을 정리하는 글입니다.</p>
</blockquote>

<h2 id="코루틴은-호출-스레드를-블록하지-않는다">코루틴은 호출 스레드를 블록하지 않는다</h2>

<hr />

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">package</span> <span class="nn">com.smp.coroutinesample.basic</span>

<span class="k">import</span> <span class="nn">kotlinx.coroutines.GlobalScope</span>
<span class="k">import</span> <span class="nn">kotlinx.coroutines.delay</span>
<span class="k">import</span> <span class="nn">kotlinx.coroutines.launch</span>


<span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">{</span>
    <span class="nc">GlobalScope</span><span class="p">.</span><span class="nf">launch</span> <span class="p">{</span>
        <span class="nf">delay</span><span class="p">(</span><span class="mi">1000L</span><span class="p">)</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"World!"</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"Hello,"</span><span class="p">)</span>
    <span class="nc">Thread</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mi">2000L</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>위 예제의 마지막 라인에는 2초간 정지하는 코드가 있다.</p>

<p><code class="language-plaintext highlighter-rouge">GlobalScope.launch {}</code> <strong>코루틴 빌더</strong>로 생성된 코루틴은 자신을 호출한 스레드를 블록하지 않기 때문에 위 예제에서 마지막 라인이 없다면 <em>World!</em> 를 출력하지 못하고 프로그램이 종료될 것이다.</p>

<p>위 예제에서 마지막 라인에 스레드를 중단하는 함수를 <code class="language-plaintext highlighter-rouge">중단 함수(Blocking function)</code>이라고 하는데 좀 더 명시적으로 나타내기 위해 다음 예제와 같이 <code class="language-plaintext highlighter-rouge">runBlocking {}</code> 블록을 사용할 수도 있다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">{</span>
    <span class="nc">GlobalScope</span><span class="p">.</span><span class="nf">launch</span> <span class="p">{</span>
        <span class="nf">delay</span><span class="p">(</span><span class="mi">1000L</span><span class="p">)</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"World!"</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"Hello,"</span><span class="p">)</span>
    <span class="nf">runBlocking</span> <span class="p">{</span>
        <span class="nf">delay</span><span class="p">(</span><span class="mi">2000L</span><span class="p">)</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">runBlocking {}</code> 블록도 <strong>코루틴 빌더</strong>인데 블록이 완료될 때까지 현재 스레드를 멈추는 코루틴을 생성한다.</p>

<blockquote>
  <p>하지만 코루틴 안에서 <code class="language-plaintext highlighter-rouge">runBlocking {}</code> 사용은 권장되지 않는다</p>
</blockquote>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="nc">GlobalScope</span><span class="p">.</span><span class="nf">launch</span> <span class="p">{</span>
        <span class="nf">delay</span><span class="p">(</span><span class="mi">1000L</span><span class="p">)</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"World!"</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"Hello,"</span><span class="p">)</span>
    <span class="nf">delay</span><span class="p">(</span><span class="mi">2000L</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>위 예제와 같이 main 함수 자체를 <code class="language-plaintext highlighter-rouge">runBlocking {}</code>으로 만들면 <code class="language-plaintext highlighter-rouge">delay()</code> 중단 함수를 더 자연스럽게 작성할 수 있다.</p>

<blockquote>
  <p><code class="language-plaintext highlighter-rouge">delay()</code>는 중단 함수이고, 모든 중단 함수는 코루틴 안에서만 호출될 수 있다.</p>
</blockquote>

<h2 id="코루틴과-타이밍-싸움-하지-말자">코루틴과 타이밍 싸움 하지 말자</h2>

<hr />

<p>위 예제에서는 <em>World!</em> 를 출력하기 위해 2초라는 임의의 시간을 잠시 멈추었다. 하지만 실제 코딩할 때 이러한 방법으로 코딩하는 것은 <strong><em>굉장히</em></strong> 좋지 않은 방법이다. 부모 코루틴에서 자식 코루틴의 작업이 얼마나 걸릴지 예상하기 힘들 뿐더러 긴 시간을 중단하게 되면 사용성도 안 좋아 질테니까.</p>

<p>이런 문제를 해결하기 위해 <code class="language-plaintext highlighter-rouge">Job 인스턴스</code>를 이용할 수 있다. <code class="language-plaintext highlighter-rouge">GlobalScope.launch{}</code>는 <code class="language-plaintext highlighter-rouge">Job 인스턴스</code>를 반환한다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">job</span> <span class="p">=</span> <span class="nc">GlobalScope</span><span class="p">.</span><span class="nf">launch</span> <span class="p">{</span>
        <span class="nf">delay</span><span class="p">(</span><span class="mi">1000L</span><span class="p">)</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"World!"</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"Hello,"</span><span class="p">)</span>
    <span class="n">job</span><span class="p">.</span><span class="nf">join</span><span class="p">()</span>
<span class="p">}</span>
</code></pre></div></div>

<p>위 예제의 마지막 라인에 <code class="language-plaintext highlighter-rouge">job.join()</code>이 있다. 여기서 <code class="language-plaintext highlighter-rouge">job</code>은 <code class="language-plaintext highlighter-rouge">GlobalScope.launch {}</code>가 반환한 <code class="language-plaintext highlighter-rouge">Job 인스턴스</code>이고, <code class="language-plaintext highlighter-rouge">join()</code>을 하면서 <code class="language-plaintext highlighter-rouge">job</code>이 종료될 때까지 대기한 후 <code class="language-plaintext highlighter-rouge">runBlocking {}</code>으로 되어있는 main() 함수가 종료될 수 있다.</p>

<p>만약 자식 코루틴이 여러 개 존재한다면 모든 자식 코루틴이 반환하는 <code class="language-plaintext highlighter-rouge">Job 인스턴스</code>를 가지고 있다가 부모 코루틴이 종료되는 시점에 모두 <code class="language-plaintext highlighter-rouge">join()</code>하여 자식 코루틴들의 종료를 기다려야 할 것이다. 이는 매우 번거로운 일이 될 수 있다. 이럴 때 필요한 것이 <strong>코루틴 스코프(Scope)</strong> 이다.</p>

<h2 id="코루틴-스코프scope">코루틴 스코프(Scope)</h2>

<hr />

<p>모든 코루틴은 각자의 스코프를 갖는다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="nf">launch</span> <span class="p">{</span>
        <span class="nf">delay</span><span class="p">(</span><span class="mi">1000L</span><span class="p">)</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"World!"</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"Hello,"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>위 예제와 같이 <code class="language-plaintext highlighter-rouge">launch {}</code> <strong>코루틴 빌더</strong> 를 사용해서 새로운 코루틴을 생성하면 부모 코루틴에 <code class="language-plaintext highlighter-rouge">join()</code>을 명시적으로 호출할 필요 없이 모든 자식 코루틴들이 실행되고 종료될 때까지 대기할 수 있다.</p>

<h2 id="runblocking-vs-coroutinescope">runBlocking vs coroutineScope</h2>

<hr />

<p>만약 어떤 코루틴을 위한 사용자 정의 스코프가 필요하다면 <code class="language-plaintext highlighter-rouge">coroutineScope {}</code> 빌더를 이용할 수 있다. 이 빌더로 생성된 코루틴은 모든 자식 코루틴이 끝날 때까지 종료되지 않는 스코프를 정의하는 코루틴이다.</p>

<p>앞서 살펴본 <code class="language-plaintext highlighter-rouge">runBlocking {}</code> 빌더와 <code class="language-plaintext highlighter-rouge">coroutineScope {}</code>의 차이는 <code class="language-plaintext highlighter-rouge">runBlocking {}</code>과 달리 <code class="language-plaintext highlighter-rouge">coroutineScope {}</code>는 자식들의 종료를 기다리는 동안 현재 스레드를 블록하지 않는다는 점이다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="nf">launch</span> <span class="p">{</span>
        <span class="nf">delay</span><span class="p">(</span><span class="mi">200L</span><span class="p">)</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"Task from runBlocking"</span><span class="p">)</span>
    <span class="p">}</span>

    <span class="nf">coroutineScope</span> <span class="p">{</span>
        <span class="nf">launch</span> <span class="p">{</span>
            <span class="nf">delay</span><span class="p">(</span><span class="mi">500L</span><span class="p">)</span>
            <span class="nf">println</span><span class="p">(</span><span class="s">"Task from nested launch"</span><span class="p">)</span>
        <span class="p">}</span>
        <span class="nf">delay</span><span class="p">(</span><span class="mi">100L</span><span class="p">)</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"Task from coroutine scope"</span><span class="p">)</span>
    <span class="p">}</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"Coroutine scope is over"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Task from coroutine scope
Task from runBlocking
Task from nested launch
Coroutine scope is over
</code></pre></div></div>

<h2 id="중단-함수-추출">중단 함수 추출</h2>

<hr />

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="nf">launch</span> <span class="p">{</span>
        <span class="nf">doWorld</span><span class="p">()</span>
    <span class="p">}</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"Hello,"</span><span class="p">)</span>
<span class="p">}</span>

<span class="k">suspend</span> <span class="k">fun</span> <span class="nf">doWorld</span><span class="p">()</span> <span class="p">{</span>
    <span class="nf">delay</span><span class="p">(</span><span class="mi">1000L</span><span class="p">)</span>
    <span class="nf">println</span><span class="p">(</span><span class="s">"World!"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>코루틴에서 함수로 추출하는 것은 비슷하지만 추출된 함수에 <code class="language-plaintext highlighter-rouge">suspend</code>라는 키워드가 붙게 된다. 이 <code class="language-plaintext highlighter-rouge">suspend</code>라는 키워드가 붙으면 중단 함수임을 뜻하게 된다.</p>

<p>중단 함수이기 때문에 코루틴 컨텍스트에서 동작하게 되고, 앞서 살펴본 <code class="language-plaintext highlighter-rouge">delay()</code>와 같은 함수도 사용할 수 있게 된다.</p>

<h2 id="코루틴은-가볍다-lightweight">코루틴은 가볍다 (lightweight)</h2>

<hr />

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="nf">repeat</span><span class="p">(</span><span class="mi">100_000</span><span class="p">)</span> <span class="p">{</span>
        <span class="nf">launch</span> <span class="p">{</span>
            <span class="nf">delay</span><span class="p">(</span><span class="mi">1000L</span><span class="p">)</span>
            <span class="nf">print</span><span class="p">(</span><span class="s">"."</span><span class="p">)</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>위 코드는 10만 개의 코루틴을 만들어서 1초 후 <code class="language-plaintext highlighter-rouge">.</code>을 찍는 코드이다. 만약 이 동작을 코루틴이 아닌 스레드에서 동작했다면 많은 메모리를 사용하고 메모리 부족 예외를 발생시킬 수도 있다.</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">(</span><span class="n">args</span><span class="p">:</span> <span class="nc">Array</span><span class="p">&lt;</span><span class="nc">String</span><span class="p">&gt;)</span> <span class="p">=</span> <span class="nf">run</span> <span class="p">{</span>
    <span class="nf">repeat</span><span class="p">(</span><span class="mi">100_000</span><span class="p">)</span> <span class="p">{</span>
        <span class="nf">thread</span> <span class="p">{</span>
            <span class="nc">Thread</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mi">1000L</span><span class="p">)</span>
            <span class="nf">print</span><span class="p">(</span><span class="s">"."</span><span class="p">)</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>]]></content><author><name>mangbaam</name></author><category term="Kotlin" /><category term="kotlin" /><category term="android" /><category term="coroutine" /><summary type="html"><![CDATA[⭐]]></summary></entry><entry><title type="html">CS 쓱 훑어보기 - 1. 디자인 패턴</title><link href="https://mangbaam.github.io//jekyll-theme-yat/cs/2022/07/01/cs-design-pattern.html" rel="alternate" type="text/html" title="CS 쓱 훑어보기 - 1. 디자인 패턴" /><published>2022-07-01T00:00:00+00:00</published><updated>2022-07-01T00:00:00+00:00</updated><id>https://mangbaam.github.io//jekyll-theme-yat/cs/2022/07/01/cs-design-pattern</id><content type="html" xml:base="https://mangbaam.github.io//jekyll-theme-yat/cs/2022/07/01/cs-design-pattern.html"><![CDATA[<p><img src="https://user-images.githubusercontent.com/44221447/176747416-43b18e95-2d6f-4783-adc4-ffe52555a2c7.png" alt="image" /></p>

<h2 id="-디자인-패턴이란">⭐ 디자인 패턴이란?</h2>

<p>디자인 패턴은 프로그램을 설계할 때 반복되는 문제점들을 해결하기 위한 일종의 규약이라고 할 수 있다.</p>

<h2 id="싱글톤-패턴">싱글톤 패턴</h2>

<hr />

<p><img src="https://user-images.githubusercontent.com/44221447/176748002-ec113d29-40ee-479a-8894-da676ed3bfa8.png" alt="싱글톤" /></p>

<p>싱글톤 패턴은 하나의 클래스가 하나의 인스턴스만을 가지는 패턴이다. 주로 DB 연결에 많이 사용된다.</p>

<p>인스턴스를 생성하는 비용이 줄어드는 장점이 있으나 의존성이 높아지는 단점이 있다.</p>

<p>의존성이 높아지면 테스트를 할 때도 걸림돌이 될 수 있다. 이때 의존성 주입(DI, Dependency Injection)으로 모듈 간의 의존성을 좀 더 느슨하게 만들 수 있다.</p>

<h2 id="팩토리-패턴">팩토리 패턴</h2>

<hr />

<p><img src="https://user-images.githubusercontent.com/44221447/176748339-403f058e-7d9e-410e-b9a2-31a57ba50db4.png" alt="팩토리패턴" /></p>

<p>팩토리 패턴은 객체를 생성하는 부분을 떼어내 추상화 한 패턴이다. 상속 관계의 두 클래스에서 상위 클래스가 추상화 된 구조를 가지고, 하위 클래스에서 이 구조를 구체화한다.</p>

<p>상위 클래스와 하위 클래스가 구분되면서 얻을 수 있는 장점은 결합이 느슨해지기 때문에 수정이 필요할 때도 유연하게 할 수 있다.</p>

<h2 id="스트래티지전략-패턴">스트래티지(전략) 패턴</h2>

<hr />

<p><img src="https://user-images.githubusercontent.com/44221447/176748812-9c380d40-34a0-4737-8341-76ad9722c4ec.png" alt="전략패턴" /></p>

<p>전략 패턴은 객체의 행위를 변경하려고 하는 경우 직접 수정하는 것이 아닌 캡슐화 된 기능을 컨텍스트 안에서 바꿔주면서 상호 교체가 가능하게 만드는 패턴이다.</p>

<p>예를 들어 결제를 해야할 때 다양한 결제 수단 중에서 하나를 고르는 것처럼 다양한 전략으로 결국 하나의 목적을 달성할 수 있도록 하는 패턴이다.</p>

<h2 id="옵저버-패턴">옵저버 패턴</h2>

<hr />

<p><img src="https://user-images.githubusercontent.com/44221447/176748934-508fd260-7bdf-4e0f-a5c0-367157acc54e.png" alt="옵저버패턴" /></p>

<p>옵저버 패턴은 어떤 객체의 상태 변화를 관찰하다가 상태 변화가 있을 때마다 옵저버 목록에 있는 옵저버에게 알려주는 패턴이다.</p>

<p>대표적으로 안드로이드의 AAC LiveData와 같은 것이 있다. 안드로이드에서는 상황에 따라 알맞은 UI를 보여주기 위해 자주 사용되는 패턴이다.</p>

<p>옵저버 패턴은 주로 MVC나 MVVM과 같은 아키텍처와 함께 사용되는 경우가 많다. 아키텍처에 대해서는 다음에 살펴볼 예정이다.</p>

<h2 id="프록시-패턴">프록시 패턴</h2>

<hr />

<p><img src="https://user-images.githubusercontent.com/44221447/176749050-ff75bb99-b281-4edf-9032-f08cf76cc4e8.png" alt="프록시패턴" /></p>

<p>프록시 패턴은 어떤 동작이나 흐름의 중간에서 가로채 특정 작업을 한 후에 넘겨주는 패턴이다.</p>

<p>예를 들어 통신 과정에서 로깅을 한다거나 캐싱을 하는 등의 일련의 작업을 한 후 원래의 목적지로 넘겨주는 경우가 있을 것이다.</p>

<p>프록시라고 하면 <strong>프록시 서버</strong>가 더 익숙할 것이다. 프록시 서버는 서버와 클라이언트가 직접 연결되는 것이 아닌 중간에 프록시 서버가 존재해 간접적으로 접속할 수 있도록 해준다.</p>

<p>보안 상의 이유로 자주 사용되는 패턴이다.</p>

<h2 id="이터레이터-패턴">이터레이터 패턴</h2>

<hr />

<p><img src="https://user-images.githubusercontent.com/44221447/176749292-0451c623-e90c-48f6-97be-013b36eb4f18.png" alt="이터레이터패턴" /></p>

<p>이터레이터 패턴은 iterator를 사용해 collection의 요소들에 접근하는 패턴이다.</p>

<p>파이썬을 예로 든다면 <code class="language-plaintext highlighter-rouge">list</code>, <code class="language-plaintext highlighter-rouge">deque</code>, <code class="language-plaintext highlighter-rouge">map object</code>, <code class="language-plaintext highlighter-rouge">tuple</code>, <code class="language-plaintext highlighter-rouge">set</code>, <code class="language-plaintext highlighter-rouge">dictionary</code> 등의 자료형이 있다. 이러한 자료형의 공통점은 순차적으로 탐색할 수 있다는 점이다. 주로 반복문과 함께 사용된다.</p>]]></content><author><name>mangbaam</name></author><category term="CS" /><category term="design pattern" /><summary type="html"><![CDATA[]]></summary></entry></feed>